# ── 필수 ────────────────────────────────────────────────
PyYAML>=6.0.1          # 설정·메타데이터 파일 읽기/쓰기
requests>=2.31.0       # 출처 자료 수집
python-dateutil>=2.9.0 # 날짜·요일 계산
markdown>=3.6          # 원고 마크다운 -> HTML 변환

# ── 선택: 이미지(표/차트) 자동 생성 ─────────────────────
# 없어도 나머지 기능은 모두 동작합니다.
matplotlib>=3.8.0
Pillow>=10.2.0

# ── 선택: 원고를 Claude API로 자동 생성 (generation.mode: api) ──
# 기본값(claude_code)에서는 필요 없습니다.
# anthropic>=1.0.0

# ── 선택: 브라우저 예약 등록 보조 기능 ──────────────────
# 기본값은 꺼져 있습니다. 사용하려면 아래 주석을 해제하고
#   python -m playwright install chromium
# 을 한 번 실행하세요.
# playwright>=1.44.0
