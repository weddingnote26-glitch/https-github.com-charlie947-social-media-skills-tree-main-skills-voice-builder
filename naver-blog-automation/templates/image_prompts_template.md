# 이미지 생성 프롬프트 및 대체 텍스트

- 게시물 ID: {{post_id}}
- 채널 색상: {{primary_color}} / 강조 {{accent_color}}
- 분위기: {{mood}}

> **지켜야 할 것**
> - 뉴스 사진을 무단으로 쓰지 않습니다.
> - 직접 만든 표, 간단한 차트, 아이콘형 설명 이미지를 씁니다.
> - 차트에는 sources.md 에서 교차확인된 수치만 씁니다.
> - AI 이미지에는 숫자·한글 문장·로고를 넣지 않습니다.
> - 파일명은 영문과 숫자로만 만듭니다.

## 1. {{image_file_1}}

- **종류**: 대표 이미지
- **삽입 위치**: 도입부 아래
- **만드는 방법**: (표/차트 자동생성 | AI 생성 | 직접 제작)
- **대체 텍스트**: {{image_alt_1}}
- **AI 생성 프롬프트** (AI로 만들 경우):
  ```
  (숫자와 글자를 넣지 않는 단순한 개념 일러스트로 작성)
  ```
- **차트 데이터** (차트일 경우 — 교차확인된 값만):
  ```
  항목,값,출처
  ```
