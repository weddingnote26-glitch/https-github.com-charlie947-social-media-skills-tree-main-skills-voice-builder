{
  "name": "naver-blog-automation",
  "version": "1.0.0",
  "private": true,
  "description": "네이버 블로그 주간 콘텐츠 제작 및 예약 발행 보조 시스템 (원고 생성 → 사실확인 → 검수 → 승인 → 예약 발행 준비)",
  "scripts": {
    "menu": "python scripts/menu.py",
    "generate": "python scripts/generate_week.py",
    "review": "python scripts/review.py",
    "prepare": "python scripts/prepare_publish.py",
    "browser:install": "playwright install chromium"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  },
  "comment": "Node는 선택적 브라우저 자동화(Playwright)에만 쓰입니다. 핵심 기능은 모두 Python으로 동작합니다."
}
