# 회사 PC에서 처음 시작하기

집에서 만든 작업물을 회사 PC로 받아오는 방법입니다.
**10분이면 끝납니다.** 한 번만 하면 그다음부터는 클릭 두 번입니다.

---

## 폴더를 두 개로 나눠 씁니다 (2026-08-22 확정)

| 폴더 | 무엇을 | 동기화 |
|---|---|---|
| `G:\내 드라이브\ORAK-작업\` | 문서·보고서·기획서 | **구글 드라이브** ✅ |
| `C:\작업\블로그작업\` | 블로그 프로젝트 (이 저장소) | **깃허브만** ✅ |

**두 폴더를 절대 겹치지 마세요.**
블로그 폴더를 드라이브 안으로 옮기거나,
드라이브 폴더 안에서 `git clone` 하시면 그때 깨집니다.

> 실수로 클라우드 안에 두면 프로그램이 **아예 실행되지 않습니다.**
> `받아오기.ps1` · `올리기.ps1` · `setup.ps1` · 메뉴 전부 막습니다.
> 안심하셔도 됩니다.

### 이미 클라우드 안에 두셨다면 — 옮기는 법

1. 프로그램과 탐색기 창을 **모두 닫습니다.**
2. `블로그작업` 폴더를 통째로 **잘라내기**(Ctrl+X) 합니다.
3. 클라우드 밖에 붙여넣기(Ctrl+V) 합니다. 예: `C:\작업\블로그작업`
4. 원드라이브를 쓰신다면, 옮긴 뒤 **원드라이브 쪽에 남은 사본을 지웁니다.**
   (안 지우면 계속 동기화됩니다)
5. 새 위치에서 `받아오기.ps1` 을 실행해 잘 되는지 확인합니다.

> **복사가 아니라 잘라내기**로 하세요.
> 두 곳에 남으면 어느 쪽이 최신인지 알 수 없게 됩니다.

---

## 왜 블로그 폴더는 구글 드라이브에 두면 안 되나요

구글 드라이브로 **블로그 폴더**를 동기화하면 작업이 깨집니다.

이 폴더 안에는 `.git` 이라는 숨은 폴더가 있습니다.
여기에 작업 기록이 수천 개의 작은 파일로 들어 있습니다.
구글 드라이브는 이 파일들을 **순서를 지키지 않고** 하나씩 올립니다.
절반만 올라간 상태에서 다른 PC가 받아 가면 기록이 망가집니다.

또 두 PC에서 같은 파일을 고치면 드라이브는 이렇게 만듭니다.

```
post.md
post (딜런의 충돌된 사본).md      ← 어느 쪽이 최신인지 알 수 없음
post (1).md
```

깃허브는 이런 일이 없습니다. **어느 쪽이 최신인지 항상 압니다.**
합칠 수 없을 때는 조용히 사본을 만드는 대신 **멈추고 물어봅니다.**

> 구글 드라이브는 사진·문서처럼 **한 사람이 한 번에 하나씩** 고치는 파일에 좋습니다.
> 그래서 `ORAK-작업` 폴더는 드라이브에 두셔도 괜찮습니다.
> 프로그램 폴더만 밖에 두시면 됩니다.

---

## 1단계 — git 설치 (5분, 한 번만)

회사 PC에 git 이 있는지 먼저 확인합니다.

**시작 버튼 → `powershell` 입력 → PowerShell 실행** 후 아래를 입력하세요.

```powershell
git --version
```

| 나온 결과 | 할 일 |
|---|---|
| `git version 2.xx.x` 처럼 나옴 | 2단계로 넘어가세요 |
| `... 인식할 수 없습니다` | 아래에서 설치하세요 |

### 설치하기

https://git-scm.com/download/win 에서 내려받아 실행합니다.
설치 중 선택지는 **전부 그냥 "Next"** 를 누르시면 됩니다.

설치가 끝나면 **PowerShell 창을 닫고 새로 여세요.** (안 그러면 계속 못 찾습니다)

> **회사에서 프로그램 설치가 막혀 있다면** → 이 문서 맨 아래 "설치가 안 될 때" 를 보세요.

---

## 2단계 — 스크립트 실행 허용 (30초, 한 번만)

PowerShell 에 아래를 그대로 붙여 넣고 Enter 를 누르세요.

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

`Y` 를 묻거든 `Y` 를 누르고 Enter.

> 이걸 안 하면 나중에 *"이 시스템에서 스크립트를 실행할 수 없으므로"* 오류가 납니다.
> 이 PC의 내 계정에서만 적용되며, 다른 사람이나 시스템에는 영향이 없습니다.

---

## 3단계 — 작업물 받아오기 (2분)

PowerShell 에 아래를 **한 줄씩** 붙여 넣으세요.

### ① 폴더 만들 곳으로 이동

```powershell
cd ~\Documents
```

### ② 받아오기

```powershell
git clone https://github.com/weddingnote26-glitch/https-github.com-charlie947-social-media-skills-tree-main-skills-voice-builder.git 블로그작업
```

`내 문서\블로그작업` 폴더가 생기고 그 안에 전부 들어옵니다.

> 로그인을 묻지 않습니다. 이 저장소는 공개 상태라 누구나 받을 수 있습니다.
> (비공개로 바꾸는 방법은 맨 아래에 있습니다)

### ③ 그 PC에 맞춰 설치

```powershell
cd 블로그작업
.\naver-blog-automation\setup.ps1
```

파이썬 환경을 만들고 필요한 것들을 설치합니다. 1~2분 걸립니다.

> 파이썬이 없다고 나오면 https://www.python.org/downloads/ 에서 설치하세요.
> 설치 화면 맨 아래 **"Add Python to PATH"** 를 꼭 체크해야 합니다.

---

## 4단계 — 끝. 그다음부터는 이렇게

`내 문서\블로그작업` 폴더를 열면 파일 두 개가 보입니다.

| 언제 | 무엇을 | 어떻게 |
|---|---|---|
| **작업 시작 전** | `받아오기.ps1` | 오른쪽 클릭 → PowerShell에서 실행 |
| **자리 뜨기 전** | `올리기.ps1` | 오른쪽 클릭 → PowerShell에서 실행 |

작업할 때는 `naver-blog-automation\run.ps1` 을 실행하면 메뉴가 나옵니다.

### 꼭 지킬 것 3가지

1. **자리를 뜨기 전에 반드시 올리기** — 안 올린 작업은 그 PC에만 남습니다.
2. **작업을 시작하기 전에 반드시 받아오기** — 최신 내용을 받고 시작합니다.
3. **한 번에 한 PC에서만** 작업합니다.

집에서 올리고 → 회사에서 받고 → 회사에서 올리고 → 집에서 받고.
이 순서만 지키면 충돌이 날 일이 없습니다.

---

## 자주 겪는 문제

**Q. `git` 을 인식할 수 없다고 나옵니다.**
설치 후 PowerShell 창을 새로 열지 않아서입니다. 창을 닫고 다시 여세요.

**Q. "이 시스템에서 스크립트를 실행할 수 없으므로" 오류**
2단계를 안 하셨습니다. 위로 올라가서 하세요.

**Q. 한글이 깨져 보입니다.**
PowerShell 창에서 `chcp 65001` 을 입력한 뒤 다시 실행하세요.

**Q. 받아오기를 했는데 집에서 한 작업이 없습니다.**
집에서 `올리기.ps1` 을 안 하셨을 수 있습니다. 집 PC에서 먼저 올려 주세요.

**Q. "충돌" 이라는 말이 나왔습니다.**
두 PC에서 같은 파일을 고쳤다는 뜻입니다.
직접 고치려 하지 마시고, Claude 에게 이렇게 말씀하세요.
```
받아오기 하다가 충돌이 났어. 화면에 나온 내용 그대로 붙여넣을게.
```

---

## 설치가 안 될 때 (회사 보안 정책)

회사 PC에서 프로그램 설치가 막혀 있거나 깃허브 접속이 차단된 경우입니다.

### 방법 A — 브라우저로 내려받기 (설치 불필요)

1. 브라우저에서 저장소 주소를 엽니다
   https://github.com/weddingnote26-glitch/https-github.com-charlie947-social-media-skills-tree-main-skills-voice-builder
2. 초록색 **`Code`** 단추 → **`Download ZIP`**
3. 받은 파일을 압축 풀기

**단점**: 받기만 됩니다. 회사에서 한 작업을 집으로 보낼 수 없습니다.
회사에서는 보기만 하고 작업은 집에서 하실 때 쓰세요.

### 방법 B — 브라우저에서 Claude 로 작업 (설치 불필요, 양방향)

**https://claude.ai/code** 에 접속해 이 저장소를 엽니다.

- 회사 PC에 아무것도 설치하지 않습니다
- 받아오기·올리기를 Claude 가 알아서 합니다
- 원고 작성·검수까지 브라우저 안에서 됩니다

회사 보안이 빡빡하다면 **이 방법이 가장 현실적입니다.**

---

## 알아두실 것 — 저장소가 공개 상태입니다

지금 이 저장소는 **공개(public)** 입니다.
주소를 아는 사람은 누구나 볼 수 있습니다.

| | |
|---|---|
| ✅ 좋은 점 | 회사 PC에서 로그인 없이 바로 받을 수 있습니다 |
| ⚠ 생각해 보실 점 | **발행 전 원고가 인터넷에 그대로 보입니다** |

네이버 계정 정보는 들어 있지 않습니다. (처음부터 올리지 않도록 막아 뒀습니다)
다만 아직 발행하지 않은 글 12편이 미리 보이는 것은 신경 쓰일 수 있습니다.

### 비공개로 바꾸려면

1. 저장소 → **Settings** 탭
2. 맨 아래 **Danger Zone** → **Change repository visibility**
3. **Make private** 선택

**바꾸신 뒤에는** 회사 PC에서 받을 때 깃허브 로그인을 한 번 물어봅니다.
`git clone` 을 실행하면 브라우저 창이 열리고, 거기서 로그인하시면 됩니다.
그 뒤로는 다시 묻지 않습니다.

> 어느 쪽이든 괜찮습니다. 편한 쪽으로 정하세요.
> 바꾸고 싶으시면 말씀해 주세요. 같이 봐 드리겠습니다.
