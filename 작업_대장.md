# 작업 대장 — 무엇이 어디에 있나

**목적: 덮어쓰기와 중복을 막습니다** (분리규칙 §3-1).

프로그램이 여러 개고 브랜치도 여러 개입니다. 어느 것이 어느 자리인지
여기 적어 둡니다. **새 작업을 시작하기 전에 이 표를 먼저 보세요.**

마지막 갱신 2026-08-29 (Stage 11)

---

## 1. 브랜치 표

| 브랜치 | 무엇 | 폴더 | 상태 |
|---|---|---|---|
| **`orak-shortform-studio`** | **오락 숏폼 AI 스튜디오 (B)** | `orak-shortform-studio/` | **지금 작업 중** |
| `claude/entertainment-short-form-studio-0wew50` | 오락 숏폼 AI 스튜디오 (B) — **먼저 쓰던 자리** | `orak-shortform-studio/` | 보관 · 손대지 않음 |
| `claude/orak-food-reel-studio-2kux9t` | 오락푸드 AI 릴스 스튜디오 | `orak-food-reel-studio/` | 별개 작업 |
| `claude/work-environment-setup-qkurkt` | 네이버 블로그 도우미 · 작업환경 | `naver-blog-automation/` | 별개 작업 |

### 지켜야 할 것

1. **다른 브랜치에 밀어 넣지 마세요.** 위 표에서 자기 줄에만 씁니다.
2. **`claude/entertainment-short-form-studio-0wew50` 는 손대지 마세요.**
   Stage 10 까지의 작업이 그대로 들어 있습니다. 되돌려야 할 때 쓰는 자리입니다.
3. 이름이 겹치면 **덮지 말고** `_2` `_3` 을 붙이고 알립니다 (§0-1 3번).

---

## 2. 왜 브랜치를 나눴나

`claude/entertainment-short-form-studio-0wew50` 하나에 계속 쌓으면
**되돌릴 자리가 없어집니다.** Stage 10 까지가 한 덩어리로 완성됐으니,
그 지점을 그대로 얼려 두고 새 자리에서 이어갑니다.

```
claude/entertainment-short-form-studio-0wew50   e5fb768 에서 멈춤 (보관)
                                                   │
                                                   └─→ orak-shortform-studio (여기서 계속)
```

두 브랜치는 **같은 역사를 공유**합니다. 새로 복사한 것이 아니라
그 지점에서 갈라져 나온 것이라, 저장 공간이 두 배로 들지 않습니다.

---

## 3. 오락 숏폼 AI 스튜디오 (B) — 어디까지 왔나

| Stage | 내용 | 상태 |
|---|---|---|
| 0·1·2 | 점검 · 설계 · 화면 | ✅ |
| 3 | 폴더 · SQLite · 열쇠 금고(DPAPI) | ✅ 시험 48개 |
| 4 | 대본 만들기 | ✅ 시험 30개 |
| 6b | 사진 움직이기 (FFmpeg) | ✅ 시험 18개 |
| 8 | 자막 + 광고 표시 | ✅ 시험 23개 |
| 컷 편집 | 사진 고르기 · 장면 배정 | ✅ 시험 25개 |
| 9 | 최종 합성 | ✅ 시험 25개 · **한 편 나옴** |
| 공급자 | 대본·이미지 OpenAI · 목소리 ElevenLabs | ✅ 시험 29개 |
| 규칙 | 기본 제작 필수 규칙 (8항목 36줄) | ✅ 시험 22개 |
| 서비스 | 단계별 실패 · 주제 · 사례 · 성과 | ✅ 시험 22개 |
| 10 | 다시 하기 · 껐다 켜도 이어서 | ✅ 시험 26개 |
| 11 | 설치파일 (PyInstaller + Inno Setup) | ✅ 시험 28개 · **윈도우에서 빌드 필요** |
| 5 | 이미지 (OpenAI) | 열쇠 넣고 모델 고르면 됨 |
| 6 | 영상 (Kling) | ⛔ **계정 확인 4가지 필요** |
| 7 | 음성 (ElevenLabs) | 열쇠 넣고 모델 고르면 됨 |
| 12 | 최종 점검 · 사용설명서 | 아직 |

**시험 345개 전부 통과.**

```
cd orak-shortform-studio
python tests/test_contracts.py  22    python tests/test_ui_flow.py    27
python tests/test_storage.py    48    python tests/test_script.py     30
python tests/test_kenburns.py   18    python tests/test_subtitles.py  23
python tests/test_photos.py     25    python tests/test_compose.py    25
python tests/test_providers.py  29    python tests/test_rules.py      22
python tests/test_services.py   22    python tests/test_recovery.py   26
python -m app.main                    창 띄우기
```

---

## 4. 확정된 것

| | 정한 것 |
|---|---|
| 자료 폴더 | `내 문서\오락 숏폼 스튜디오\` |
| 대본 | OpenAI (모델은 설정에서 고름) |
| 이미지 | OpenAI (모델은 설정에서 고름) |
| 목소리 | ElevenLabs (모델·목소리는 설정에서 고름) |
| 영상 | Kling — 계정 확인 뒤 |
| SQLite 표 | 기존 5개 + 덧붙인 4개 |

**모델 이름은 코드에 한 개도 없습니다.** 설정 화면의 [연결 확인] 이
계정에서 받아온 목록에서 고릅니다 (§0-2).

---

## 5. 절대 건드리지 않는 곳 (분리규칙 §2 · §3)

```
바탕화면\오락_당근_콘텐츠\        A 프로그램 (당근 카드뉴스)
바탕화면\오락_당근_배포도구\      A 프로그램
바탕화면\오락_숏폼스튜디오\       예전 자리
바탕화면\오락이 마스터 파일\      원본 그림
```

프로그램 코드가 막고 있습니다 (`app/core/paths.py` 의 `assert_writable`).
시험이 네 곳 모두 막히는지 확인합니다.

**지우는 코드는 없습니다.** 유일한 `DELETE` 는 담당자가 「기본 제작 필수 규칙」
한 줄을 지울 때뿐이고, 그것도 시험이 확인합니다.

---

## 6. 이어서 할 때

Claude 에게 이렇게 말씀하세요.

```
작업_대장.md 읽고 오락 숏폼 이어서 해줘
```

더 자세한 진행 기록은 [WORKLOG.md](WORKLOG.md) 에 있습니다.
