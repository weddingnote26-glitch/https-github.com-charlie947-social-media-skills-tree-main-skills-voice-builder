# 오락_당근_컨텐츠 자동 생성 프로그램 — 사장님 결정사항 (2026-08-31)

> **이 문서의 목적**
> 사장님이 2026-08-31 회신에서 확정하신 내용을 그대로 옮겨 적은 것입니다.
> 집·회사 어느 PC에서 이어서 작업하든 이 결정이 사라지지 않게 하려고 남깁니다.
> **작업을 시작하기 전에 이 문서를 먼저 읽으십시오.**

---

## ★ 남은 할 일 (여기부터 보십시오)

| # | 할 일 | 급한가 | 자세한 내용 |
|---|---|---|---|
| 1 | **`경로검사_v2.py` 버그 고치기 — `[A-D]` → `[A-E]`** | 급하지 않음 | [§5](#5-경로검사_v2py-버그--급하지-않음) |
| 2 | 2단계 — 프로젝트 뼈대 · `.venv` · `requirements.txt` | **다음 차례** | [§7](#7-남은-단계와-통과-기준) |
| 3 | 3단계 — `renderer.py` 이식, 카드가 예전과 똑같이 나오는지 검증 | **가장 중요한 관문** | [§7](#7-남은-단계와-통과-기준) |
| 4 | 4단계 — 샘플 1편 생성 후 실제 토큰·원화 비용 보고 | 3단계 통과 후 | [§7](#7-남은-단계와-통과-기준) |

### 1번 — `경로검사_v2.py` 버그 (한 글자만 고치면 됩니다)

```python
m = re.search(r"PROJECT\s*=\s*([A-D])", txt)     # 현재 — E 를 못 읽습니다
m = re.search(r"PROJECT\s*=\s*([A-E])", txt)     # 이렇게 고치십시오
```

- **고칠 곳**: 정본 `<드라이브>\내 드라이브\오락_당근_배포도구\경로검사_v2.py` — **여기 하나만**
- **고치지 말 곳**: 바탕화면 사본 `C:\Users\admin\Desktop\오락_당근_배포도구\` — 그냥 둡니다
- 고친 뒤 E(게시 도우미 앱) 폴더에서 한 번 돌려 `PROJECT=E` 가 읽히는지 확인하십시오

### 하지 말 것

| 하지 말 것 | 사유 |
|---|---|
| 구 규약 14편 변환 (`02_재테크공부` 7편 · `_동네생활_모객` 7편) | **취소됨.** `ContentScanner.kt` 가 두 형식을 모두 읽음 → [§2](#2--취소된-작업--구-규약-14편-변환-하지-말-것) |
| 견본 3편 다시 만들기 · `_견본` 폴더 다시 만들기 | 이미 있음 → [§1](#1-견본-3편--이미-있음-다시-만들지-말-것) |
| 결과물 업로드 기능 만들기 | 콘텐츠 루트가 이미 드라이브라 저장 즉시 공유됨 → [§6](#6-미결-3가지--확정-다시-묻지-말-것) |
| 바탕화면 사본 폴더에서 작업하거나 삭제 | 정본은 드라이브 → [§4](#4-배포도구-정본--구글-드라이브) |

---

## 이 문서를 만든 세션이 본 것 / 못 본 것

| 구분 | 내용 |
|---|---|
| 이 세션이 볼 수 있는 것 | 깃허브 저장소 하나뿐 (클라우드 컨테이너) |
| 이 세션이 **못 보는** 것 | 사장님 PC, 구글 드라이브, 바탕화면, 견본 JSON, `orak_cards.py`, `ContentScanner.kt`, 지시서 |
| 이 세션이 만든 문서 | `오락_당근_1단계_조사보고서.md`, 이 문서 — **그 외 없음** |

사장님이 회신에서 정정하신 §5·§12 가 담긴 **인수인계 문서는 이 세션이 만든 것이 아닙니다.**
사장님 PC에서 돌던 다른 세션의 산출물입니다. 그 세션에서 이어서 작업하셔야 합니다.

---

## 1. 견본 3편 — 이미 있음. 다시 만들지 말 것

```
<드라이브>\내 드라이브\오락_당근_콘텐츠\03_뉴스 일상공유\content\_견본\03_뉴스 일상공유_견본.json   4,887 B
<드라이브>\내 드라이브\오락_당근_콘텐츠\04_좋은글 모음\content\_견본\04_좋은글 모음_견본.json      4,151 B
<드라이브>\내 드라이브\오락_당근_콘텐츠\06_취업 창업 교육강좌\content\_견본\06_취업 창업 교육강좌_견본.json  5,244 B
```

- 2026-08-31 23:25~23:27 에 사장님이 직접 넣으셨습니다.
- `_견본` 폴더도 그때 함께 생성되었습니다.
- **다시 만들지 마십시오. 덮어쓰지 마십시오.**

---

## 2. 🔴 취소된 작업 — 구 규약 14편 변환 (하지 말 것)

이전 인수인계 문서 §5 의 판단은 **틀렸습니다.**

> (틀린 판단) 구 규약 `1_썸네일.png` · `2~5.png` · `본문.txt` 는 E 앱이 못 읽는다

E 의 `ContentScanner.kt` 는 **두 세대를 모두 읽도록 처음부터 설계**되어 있습니다.

```
게시물 폴더 판정: 이미지 3~5장 + (제목.txt 또는 본문.txt 또는 post.txt) 중 하나 이상.
 - 기존 형식: 1_썸네일.png, 2.png… + 제목.txt, 본문.txt, !출처.txt,
              !게시전확인.txt, 댓글질문.txt, 모객용_동네생활.txt
 - 엔진 출력 형식: 01.jpg~05.jpg + post.txt, sources.txt, 댓글질문.txt,
              다음주제.txt (preview.html 은 무시)
```

```kotlin
val IMAGE_EXTENSIONS = setOf("png", "jpg", "jpeg", "webp")
```

**결정**

| 항목 | 결정 |
|---|---|
| `02_재테크공부` 7편 변환 | **취소. 하지 말 것** |
| `_동네생활_모객` 7편 변환 | **취소. 하지 말 것** |
| 미결 항목(§12)에서 | **삭제** |
| 새로 만드는 콘텐츠 | **반드시 E 규약(엔진 출력 형식)으로 생성** — 이건 그대로 유지 |

---

## 3. 게시판 이름 — 파일 증거 채택

| 대장 표기 | 이전 `[가정]` | 확정 | 근거 |
|---|---|---|---|
| 일상정보 공유방 | `03_뉴스 일상공유` | **`05_생활정보`** | `05_생활정보\content\2026-08\3주차\2026-08-20.json` 안에 `"게시판": "일상정보 공유방"` 실재 |
| 일상 이야기 (1건) | — | **`[가정]` 유지** | 대응 파일 없음. `04_좋은글 모음` 성격으로 추정 |

- **파일 증거가 추측보다 우선합니다.**
- `일상 이야기` 분류는 **통계·표시용으로만** 씁니다. 중복 검사는 **대장 전체를 대조**하므로 분류가 틀려도 중복 판정에 영향이 없습니다.
- `중복금지_규칙` 키 이름 정정도 반영합니다.

---

## 4. 배포도구 정본 = 구글 드라이브

```
정본  <드라이브>\내 드라이브\오락_당근_배포도구\        ← 여기서만 작업
사본  C:\Users\admin\Desktop\오락_당근_배포도구\        ← 작업 안 함. 지우지도 않음
```

- `정본=예` / `정본=아니오` 표시 방식 **그대로 유지**.
- 바탕화면 사본에 `경로검사_v2.py` (9,989 B) 가 하나 더 있습니다. 정본이 바뀌기 전에 들어간 것입니다.
  **그냥 두십시오. 그 폴더에서는 아무 작업도 하지 않습니다.**

---

## 5. `경로검사_v2.py` 버그 — 급하지 않음

```python
m = re.search(r"PROJECT\s*=\s*([A-D])", txt)     # 현재 (E 를 못 읽음)
m = re.search(r"PROJECT\s*=\s*([A-E])", txt)     # 이렇게 고칠 것
```

- 증상: E(게시 도우미 앱) 폴더 검사 시 `PROJECT=E` 를 인식하지 못함
- 영향: **A 작업에는 지장 없음.** 손이 닿을 때 고치면 됨
- 고칠 곳: **정본(드라이브) 쪽만.** 바탕화면 사본은 건드리지 않음
- 이 문서 맨 앞 **[★ 남은 할 일](#-남은-할-일-여기부터-보십시오) 1번** 과 같은 항목입니다

### 파일명 규칙 (유지)

문서 오탐을 막기 위해 문서 파일 이름에 **`개발지시`** 또는 **`인수인계`** 를 넣습니다. 이 규칙을 계속 지킵니다.

---

## 6. 미결 3가지 — 확정. 다시 묻지 말 것

| 질문 | 확정된 답 |
|---|---|
| 33건을 한 번에 뽑을지, 게시판별로 나눌지 | **게시판별로 나눔.** 한 게시판을 끝내고 다음으로. 캐싱이 깨지면 비용이 오름 (§4 규칙 그대로) |
| 담당자가 보류 폴더를 지나칠 가능성 | 완료 화면에 **"○편은 보류 폴더에 있습니다"를 빨간 글씨로 크게** 표시. **보류가 1건이라도 있으면 보류 폴더를 함께 연다** |
| 결과물을 드라이브에 올릴지 | **별도 업로드 기능을 만들지 말 것.** 콘텐츠 루트가 이미 드라이브라 저장하는 순간 자동 공유됨 |

### 함께 승인된 것

- 원고 JSON 에서 `Day` · `본문규칙` 키 **제외**
- `_로그\` 폴더 **자동 생성**
- `실행중.lock` 방식 **채택**
- 드라이브 스트리밍 실패 시 **재시도 금지**

---

## 7. 남은 단계와 통과 기준

**각 단계 끝에서 멈추고 보고할 것.**

| 단계 | 할 일 | 통과 기준 |
|---|---|---|
| **2** | 프로젝트 뼈대 · `.venv` · `requirements.txt` | **전역 pip 설치 0건.** `.venv` 안에만 설치 |
| **3** | `renderer.py` 이식 (`orak_cards.py` 복사 + 폰트) | **기존 원고 JSON 1편(01 또는 05)을 넣으면 카드 5장이 예전과 똑같이 나온다** |
| **4** | 샘플 1편 실제 생성 | **실제 토큰 사용량과 원화 비용 보고** |

> **3단계가 이 프로젝트에서 가장 중요한 관문입니다.**
> 카드가 예전과 다르게 나오면, 원인을 찾기 전에는 다음으로 가지 마십시오.

### 폰트 규칙

| 상황 | 규칙 |
|---|---|
| 개발 중 시험 | `orak_cards.py` 의 맑은 고딕 자동 대체 안전장치 덕분에 폰트 파일 없이도 동작 |
| 배포 EXE | **반드시 `assets\fonts\` 의 Noto Sans KR 사용** (맑은 고딕은 재배포 라이선스 없음) |
| 확인 방법 | 대체가 일어나면 콘솔에 `[알림] … 로 대체합니다` 출력 |

> **3단계 확인 때 `[알림] … 로 대체합니다` 줄이 보이면 폰트가 없다는 뜻입니다. 반드시 보고하십시오.**

### 4단계 비용 보고 양식

```
입력 토큰 / 출력 토큰 / 캐시 적중 토큰
1편 비용(원)  ·  33건 환산(원)  ·  월 환산(원)
지시서의 [가정] 월 20,100원과 얼마나 다른가
```

---

## 8. 이 작업을 어디서 이어서 할 것인가

2~4단계는 **사장님 PC의 실제 파일이 있어야** 할 수 있습니다.

| 필요한 것 | 어디에 있나 | 클라우드 세션에서 보이나 |
|---|---|---|
| `orak_cards.py` (3단계 이식 원본) | 사장님 PC | ❌ |
| 원고 JSON (3단계 시험 입력) | 구글 드라이브 | ❌ |
| 예전 카드 5장 (3단계 비교 대상) | 사장님 PC | ❌ |
| 지시서 (§6 폰트 규정 등) | 사장님 PC | ❌ |
| `.venv` | 윈도우 전용 (`Scripts\python.exe`) | ❌ 여기서 만들어도 전달 안 됨 |

**따라서 2~4단계는 사장님 PC에서 돌던 세션에서 이어서 하십시오.**

클라우드(깃허브) 세션에서 이어서 하시려면, 먼저 `당근폴더_가져오기.ps1` → `올리기.ps1` 로
당근 폴더를 깃허브에 올려 주셔야 합니다.
