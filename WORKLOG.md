# 작업 일지

자리를 옮기기 전에 아래 형식으로 한 칸씩 추가하고 push 하세요.
다음 자리에서 이 파일만 읽으면 어디서부터 이어가면 되는지 알 수 있습니다.

---

## 2026-08-29 (토) — 오락 숏폼 · Stage 3 · 4 · 6b · 8 · 컷편집 · 9

> **이어서 하실 때:** **"WORKLOG 읽고 오락 숏폼 이어서 해줘"**
> 브랜치 `claude/entertainment-short-form-studio-0wew50` · 폴더 `orak-shortform-studio/`

### 어디까지 했나

| Stage | 내용 | 상태 |
|---|---|---|
| 0·1·2 | 점검 · 설계 · 화면 | ✅ |
| **3** | 폴더 · SQLite · 열쇠 금고(DPAPI) | ✅ 시험 45개 |
| **4** | 대본 만들기 (claude-opus-5) | ✅ 시험 30개 |
| **6b** | 사진 움직이기 (FFmpeg) | ✅ 시험 18개 · **진짜 영상 나옴** |
| **8** | 자막(ASS) + 광고 표시 | ✅ 시험 23개 · **진짜 구워서 확인** |
| **컷 편집** | 사진 고르기 · 장면 배정 · 길이 조정 | ✅ 시험 25개 |
| **9** | 최종 합성 (이어붙이기 · BGM · 자막 굽기) | ✅ 시험 25개 · **한 편 나옴** |
| 5 | 이미지 (Gemini) | ⛔ **요청 형식·요금 미확인 — 막힘** |
| 6 | 영상 (Kling) | ⛔ **계정 확인 필요 — 막힘** |
| 7 | 음성 (ElevenLabs) | 한국어 모델 답 기다리는 중 |
| 10~12 | 다시하기 · 설치파일 · 최종점검 | 아직 |

**시험 215개 전부 통과.**

```
cd orak-shortform-studio
python tests/test_contracts.py   22
python tests/test_ui_flow.py     27
python tests/test_storage.py     45
python tests/test_script.py      30
python tests/test_kenburns.py    18
python tests/test_subtitles.py   23
python tests/test_photos.py      25
python tests/test_compose.py     25
python -m app.main               창 띄우기
```

### 시험이 잡은 진짜 결함 (기록해 둡니다)

1. **참고 URL·메모가 마스킹을 안 거쳤습니다.** 담당자가 토큰이 든 주소를
   붙여넣으면 DB 에 평문으로 남았습니다. MVP 판정 18번 위반이었습니다.
   → 담당자가 넣는 모든 자유 입력이 마스킹을 거치게 고쳤습니다.
2. **`has_filter` 가 없는 필터를 있다고 답했습니다.** `ffmpeg -h filter=없는것` 은
   종료코드 0 을 냅니다. 출력을 봐야 했습니다.
3. **Stage 2 옛 시험 2개**가 필수 칸 검사가 생기면서 깨졌습니다. 새 동작이 맞아
   시험 쪽을 고쳤습니다.
4. **하단 문구가 「Stage 2」 로 낡아** 있었습니다. 갈아끼울 수 있게 바꿨습니다.
5. **자막 배경 띠 여백이 설정 파일을 무시**하고 6px 로 박혀 있었습니다.
   고쳐서 키웠더니 ASS 의 불투명 상자가 **줄마다 따로 그려져** 계단처럼
   어긋나는 게 드러났습니다. → 띠를 직접 그려 한 덩어리로 바꿨습니다.
6. **사진 이름이 입력칸처럼 보였습니다.** QLabel 이 QFrame 을 물려받아서
   `QFrame {}` 스타일이 안쪽 글자에도 테두리를 그렸습니다.
7. **버튼 글자가 잘렸습니다** — 「위로」 가 「귀토」 로. 너비를 좁게 고정한 탓입니다.
   ▲▼ 기호도 글꼴에 없어 막대로 보여 한글로 바꿨습니다.
8. **배경음악이 사실상 안 들렸습니다.** 원인이 둘이었습니다 (Stage 9).
   ① `amix` 는 기본값이 **입력 개수로 나누기** 라서 말소리와 BGM 이 나란히
   6dB 씩 작아졌습니다 → `normalize=0`.
   ② 「voice 대비 -18dB」 를 **음원 파일에 -18dB** 로 잘못 구현했습니다.
   넣는 음원마다 원래 크기가 달라서, 조용한 mp3 는 안 들리고 시끄러운 mp3 는
   말소리를 덮었습니다 → **먼저 재고**(LUFS) 목표에 맞춥니다.
   자세한 내용과 숫자는 `STAGE9_합성.md` §5.

### ⛔ 사장님 답을 기다리는 것

**충돌 2가지 (2026-08-29 새 지시서 vs 기존 지시서·분리규칙)**

| | 지금 | 새 지시서 |
|---|---|---|
| 데이터 폴더 | `내 문서\ORAK_SHORTFORM_STUDIO\` (§10-1 · 분리규칙 §2) | `%LOCALAPPDATA%\OrakShortformStudio\` |
| SQLite 표 | 5개 (§10-2) — Stage 4 가 쓰는 중 | 4개 |

`%LOCALAPPDATA%` 는 숨김 폴더라 담당자가 영상을 못 찾습니다.
절충안: 영상·프로젝트는 내 문서, DB·캐시·임시는 LOCALAPPDATA.

**Stage 별로 필요한 것**

| 질문 | 언제 |
|---|---|
| Gemini 이미지 요청 형식 · 장당 요금 | Stage 5 전 |
| Kling 계정 4가지 (발급·잔액·동시처리·2.6 권한) | Stage 6 전 |
| ElevenLabs 한국어 모델 | Stage 7 전 |
| BGM 조달처 (상업적 이용 가능한 것) | 지금 — 폴더만 비어 있습니다 |

### 사장님이 해주셔야 하는 것

1. **마스터 이미지 3장** — 저장소를 **비공개로 바꾼 뒤** `assets/master/` 에 넣고
   `.gitignore` 차단 두 줄 지우기 (순서 중요 · 안내문은 그 폴더에)
2. **대본 열쇠** — console.anthropic.com 에서 **카드뉴스와 다른 키** 발급 →
   설정 → 「대본 만들기 (Claude)」 → [저장]. 콘솔에서 지출 한도도 거세요
3. **자막 글꼴** — fonts.google.com/noto/specimen/Noto+Sans+KR 에서 받아
   `assets/fonts/NotoSansKR-Bold.ttf` 로 넣어주세요 (OFL · 재배포 가능).
   맑은 고딕은 재배포 권리가 없어 못 씁니다.
4. **배경음악** — 상업적 이용이 되는 음원을 `assets/bgm/` 에 넣어주세요.
   **크기는 아무래도 상관없습니다** — 프로그램이 재서 맞춥니다.
   비워 두면 배경음악 없이 만들어집니다 (영상·자막은 그대로 나옵니다).

### 알아두실 것

**Stage 7(음성) 전까지 미리보기는 소리가 작게 들립니다.** 고장이 아닙니다.
배경음악은 「말소리보다 18dB 아래」 로 맞춰져 있는데 아직 말소리가 없어서
그렇습니다. 음성이 붙는 순간 말소리가 올라오고 배경음악은 그 아래에 깔립니다.

### 윈도우 PC 설치

```powershell
cd $HOME\Documents
git clone https://github.com/weddingnote26-glitch/https-github.com-charlie947-social-media-skills-tree-main-skills-voice-builder.git 블로그작업
cd 블로그작업
git checkout claude/entertainment-short-form-studio-0wew50
cd orak-shortform-studio
py -3.13 -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install PySide6==6.8.1 pywin32 Pillow anthropic imageio-ffmpeg
python -m app.main
```

당근 카드뉴스(A) 와 venv 를 따로 씁니다 (분리규칙 §3-7).

---

## 2026-08-28 (금) — 오락 숏폼 AI 스튜디오 (신규 · Stage 0~2)

> **이어서 하실 때:** Claude 에게 **"WORKLOG 읽고 오락 숏폼 Stage 3 이어서 해줘"** 라고 하세요.
> 브랜치: `claude/entertainment-short-form-studio-0wew50`
> 폴더: `orak-shortform-studio/`

### 새 프로그램을 시작했습니다

「동네친구 오락 — 만두탐정 오락이」 릴스·쇼츠를 만드는 **Python + PySide6** 프로그램입니다.
기존 `orak-food-reel-studio`(Node) 와는 **다른 프로그램**이며, 그 폴더는 한 글자도 건드리지 않았습니다.

분리규칙 문서(A 당근 카드뉴스 / B 오락 숏폼)를 받아 그대로 따르고 있습니다.
이 폴더는 **B** 이고, A(`바탕화면\오락_당근_*`)는 접근 금지입니다.

### 어디까지 했나

| Stage | 내용 | 상태 |
|---|---|---|
| 0 | 환경 점검 | ✅ `STAGE0_현황보고.md` |
| 1 | 인터페이스 설계 | ✅ `STAGE1_설계.md` · 계약 시험 22개 |
| 2 | 화면 뼈대 (메뉴 4개) | ✅ `STAGE2_화면.md` · 화면 시험 16개 |
| **3** | **폴더·SQLite·키 금고(DPAPI)** | **← 다음에 할 것** |
| 4~12 | 대본·이미지·영상·음성·자막·합성·설치파일 | 아직 |

**시험 38개 전부 통과합니다.**

```
cd orak-shortform-studio
python tests/test_contracts.py     계약 22개
python tests/test_ui_flow.py       화면 16개
python -m app.main                 창 띄우기 (PySide6 설치 필요)
```

### 아직 화면만 있습니다

버튼을 눌러도 실제로는 아무것도 만들지 않습니다. 대본과 작업목록은 미리 넣어둔 예시입니다.
**캐릭터 화면만 진짜**로 `assets/character_profile.json` 을 읽습니다.

### 사장님이 해주셔야 하는 것 2가지

**1. 마스터 이미지 3장 — 순서가 중요합니다**

```
① 저장소를 비공개로 바꾸기   (github → Settings → 맨 아래 Danger Zone → Private)
② assets/master/ 에 3장 넣기
③ .gitignore 에서 마스터 이미지 차단 두 줄 지우기
④ 올리기.ps1
```

지금 저장소가 **공개**라, 한 번 올라가면 나중에 비공개로 바꿔도 되돌릴 수 없습니다.
그래서 실수로 올라가지 않게 막아두었습니다. 자세한 순서는
`orak-shortform-studio/assets/master/여기에_넣어주세요.txt` 에 있습니다.

**2. 색깔 6칸**

`assets/character_profile.json` 을 메모장으로 열면 `__사장님_확인_필요__` 가 6개 있습니다.
모자·코트·리본·단추·신발·만두 피부가 각각 무슨 색인지 적어주세요.
Claude 는 이미지를 볼 수 없어서 모릅니다. 비어 있으면 장면마다 색이 달라집니다.

### 답을 기다리는 것 (해당 Stage 직전에 필요)

| 질문 | 언제 |
|---|---|
| 대본에 쓸 Claude 모델명 (카드뉴스와 **다른 키**) | Stage 4 전 |
| Gemini 이미지 요청 형식과 장당 요금 | Stage 5 전 |
| Kling 계정 4가지 (발급 가능? 잔액? 동시처리 개수? 2.6 권한?) | Stage 6 전 |
| ElevenLabs 한국어 모델 | Stage 7 전 |
| BGM 어디서 구할지 | Stage 9 전 |

**Stage 3 은 위 질문 없이 진행할 수 있습니다.**

### 정직하게 적어둘 것

- **Stage 0 에서 "화면(PySide6) 실행 시험 불가" 라고 한 것은 틀렸습니다.**
  Qt 를 화면 없이 띄우는 방법이 있어서 실제로 눌러보고 사진까지 찍었습니다.
- **여전히 못 하는 것:** 윈도우 키 금고(DPAPI) · 설치파일 만들기 ·
  Kling·Gemini·ElevenLabs 실제 호출 (작업 환경에서 막혀 있음).
  이건 사장님 PC 에서 확인하셔야 합니다.
- 지시서의 숫자 둘이 안 맞습니다 — "Kling 이 86~90%" 는 클립 **3개** 기준이고,
  기본값 2개면 59% 입니다. 동작에는 문제없고 금액은 `pricing.json` 에서 읽습니다.

### 다음에 할 일

1. **Stage 3** — 폴더 만들기 · SQLite 5개 표 · DPAPI 키 금고 · 마스킹 함수 단위시험
2. DPAPI 는 윈도우 전용이라 금고를 갈아끼울 수 있는 구조로 만들 예정
3. Stage 3 이 끝나면 멈추고 확인받기

---

## 2026-08-24 (월) — 회사 · 3차 (Cloudflare FLUX 이미지 공급자)

### 추가한 것 — 무료 사용량으로 이미지 만들기

설정 → 이미지 생성 공급자에 **Cloudflare FLUX** 가 생겼습니다.
OpenAI 크레딧이 없어도 Cloudflare 무료 사용량으로 이미지를 만들 수 있습니다.

- **장면별 자동 라우팅** — 오락이가 나오는 장면(§16 원칙상 9장 중 4장)은
  기준 이미지를 받는 캐릭터 모델(SDXL-lightning)로, 음식·매장·배경 장면은
  빠르고 싼 FLUX-1-schnell 로 갑니다. 모델은 설정에서 바꿀 수 있습니다.
- **오락이 Identity Lock** — 캐릭터 장면에는 master 참조 이미지 + 생김새 고정
  프롬프트 + negative prompt 가 자동으로 붙습니다. negative 를 못 받는 모델이면
  금지 규칙을 본문에 넣습니다.
- **429 = 무료 사용량 소진** — 재시도하지 않고 즉시 대체 공급자로 넘어가며,
  그날은 그 공급자를 다시 부르지 않습니다.
- **대체 순서** Cloudflare → Gemini → OpenAI → Sample. 설정에서 켜고 끌 수 있습니다.
- **이미지 재사용(캐시)** 도 설정에서 켜고 끕니다. 캐시 키에 공급자·캐릭터 여부·
  참조 이미지까지 들어가 공급자를 바꾸면 새로 만듭니다.
- 이미지마다 `scene-01.json` 으로 **어떻게 만들었는지**(공급자·모델·장면 유형·
  대체 여부·프롬프트) 남습니다.
- 연결 테스트가 **계정에서 실제로 쓸 수 있는 모델 목록**을 물어봐서, 설정된
  모델이 그 계정에서 안 되면 알려 줍니다. 401/403/429 도 각각 다음 할 일을 안내합니다.

### 확인한 것

- 시험 19개 추가 (응답 형태 2종·429 보호·값 빼고 재시도·라우팅·캐시·대체 순서)
  — 전체 240개 통과, 빌드·타입 검사·설치본 비밀값 검사 통과
- **릴스 E2E**: 신림동 오첨지 / 만두탐정 오락이 — 9단계 전부 통과,
  27.3초 MP4 실제 렌더링, Cloudflare 호출 캐릭터 4회 / FLUX 5회
- 429 시험에서 **공용 재시도가 429 를 다시 보내던 버그**를 잡아 고쳤습니다

### 못 한 것 (정직하게)

- **진짜 Cloudflare 계정으로는 못 눌러 봤습니다.** 이 작업 환경에서
  api.cloudflare.com 이 막혀 있습니다. 응답 모양은 공개 문서·사례 기준으로
  맞췄고 모형 응답으로 검증했지만, 실제 계정 연결은 사용자 PC 에서
  [연결 테스트]로 확인해야 합니다.
- SCENE_ROLE_MAP 이 §16(음식 60%)과 달리 9장 중 8장에 오락이를 넣고 있어
  4장으로 맞췄습니다 — 캐릭터 장면 수가 이미지 사용량과 직결되기 때문입니다.

### 쓰는 법

1. dash.cloudflare.com 가입 → 첫 화면 오른쪽 **Account ID** 복사
2. 내 프로필 → API Tokens → **Workers AI 권한**으로 토큰 생성
3. 설정 → 이미지 생성 → 공급자 **Cloudflare FLUX** → 두 값 넣고 저장 → [연결 테스트]

---

## 2026-08-24 (월) — 회사 · 2차 (Instagram 연결 성공 · 갈래 사고)

### ✅ Instagram 연결이 됐습니다

    ✅ 연결 성공 — @orak_food (Instagram 로그인)

막힌 원인이 **두 가지가 겹쳐** 있었습니다.

**① 토큰을 엉뚱한 서버로 보내고 있었습니다**

Meta 는 Instagram 발행 로그인 방식이 두 가지고 주소가 다릅니다.

| 로그인 방식 | 토큰 모양 | 물어볼 주소 |
|---|---|---|
| Instagram 로그인 | `IGAA…` | `graph.instagram.com` |
| 페이스북 로그인 | `EAA…` | `graph.facebook.com` |

프로그램은 `graph.facebook.com` 만 알고 있었습니다. 그래서 멀쩡한 토큰에
`Cannot parse access token` 이 돌아왔고, 그 문구를 보면 누구나 토큰이
잘못됐다고 생각합니다. 토큰을 몇 번을 다시 만들어도 같은 화면만 나옵니다.

**② 고친 코드가 회사 PC 로 오지 않고 있었습니다**

회사 PC 가 `claude/work-environment-setup-qkurkt` 갈래를 보고 있었는데,
릴스 스튜디오 작업은 `claude/orak-food-reel-studio-2kux9t` 에만 올라가고
있었습니다. `업데이트.bat` 은 "지금 보고 있는 갈래" 를 받아오므로 아무리
눌러도 새 작업이 하나도 오지 않았습니다.

화면에는 갈래 이름이 계속 찍히고 있었는데 아무도 그걸 보지 않았습니다.
"업데이트했는데 화면이 그대로다" 를 **다섯 번** 반복한 뒤에야 찾았습니다.

### 다시 같은 일이 없도록

- **`업데이트.bat` 이 갈래를 스스로 확인하고 옮깁니다.**
  받은 뒤 무엇을 받았는지 커밋을 한 줄 찍습니다.
- **설정 → [ℹ️ 프로그램 정보]** 에 빌드 시각·커밋·실행 방식이 나옵니다.
  `업데이트.bat` 이 찍은 커밋과 대조하면 반영됐는지 바로 알 수 있습니다.
- 연결 테스트 실패 문구 끝에 어느 서버에 물어봤는지 붙습니다
  (`[graph.instagram.com]`).

### 폴더를 나눴습니다

    C:\orak                              ← 릴스 스튜디오 (여기서만 씁니다)
    Documents\블로그작업                  ← 네이버 블로그 (그대로 둡니다)

한 폴더는 갈래 하나만 볼 수 있는데, 두 프로젝트가 서로 다른 갈래에서
돌고 있어 계속 부딪혔습니다. 겸사겸사 구글 드라이브 동기화 폴더 밖으로
나와서 빌드 중 EPERM 도 없어집니다.

### 그 밖에 고친 것

- **업데이트가 EPERM 하나로 통째로 멈추던 것** — `.next` 지우기는 준비
  작업일 뿐인데 실패하면 업데이트 전체를 포기했습니다. 이제 ① 기다렸다
  다시 → ② 이름 바꿔 치워 두기 → ③ 그냥 진행 순으로 견딥니다.
- **클라우드 동기화 폴더 경고** — 경로에 이름이 없어도(구글 드라이브가
  Documents 를 제자리 동기화하는 경우) 알아채도록 프로그램 실행 여부까지 봅니다.
- **영상 공개 주소 칸** — "설정에서 입력하세요" 라고 안내하면서 정작 칸이
  없었습니다. 내 PC 주소(localhost·192.168.…)는 저장 단계에서 막습니다.
- **빌드 뿌리 고정** — 개인 폴더의 엉뚱한 package-lock.json 을 뿌리로
  짐작하던 경고를 없앴습니다.

### 다음에 할 일

1. **ElevenLabs 목소리를 `기본` 표시 있는 것으로 바꾸기**
   무료 요금제는 Voice Library 목소리를 API 로 못 씁니다 (402)
2. OpenAI 크레딧 충전 (지금은 이미지가 자리표시자로 대체됩니다)
3. **릴스 1편 끝까지 만들어 보기** ← 발행 말고 제작까지만
4. 그다음에 영상 공개 주소(Cloudflare Tunnel 등) → 실제 발행
5. 집 PC 도 `C:\orak` 로 새로 받기 (같은 갈래 문제가 있습니다)

---

## 2026-08-24 (월) — 회사 (설치형 앱 · Instagram 안내 · 설정 화면 정렬)

### 고친 것

- **Instagram 연결 테스트가 `응답 400 — 토큰/권한을 확인하세요` 하나만 보여주던 것**

  Meta 는 서로 다른 세 가지를 모두 400 으로 돌려줍니다. 고쳐야 할 곳이 다른데
  한 문장으로 뭉뚱그리니, 맞는 값을 넣어 두고도 토큰만 계속 다시 발급받게 됩니다.
  응답 안의 `error.code` / `error_subcode` 를 읽어 구분합니다.

  | 원인 | 이제 나오는 안내 |
  |---|---|
  | 토큰 만료 (190/463) | 장기 토큰(60일)으로 바꾸는 방법 |
  | 권한 누락 (10/200) | 필요한 권한 이름 — 토큰을 새로 만들라고 하지 않음 |
  | 계정 ID 오류 (100/33) | 페이스북 페이지 ID 와 헷갈린 경우 + 올바른 ID 찾는 법 |
  | 요청 한도 (4/17/32/613) | 잠시 뒤 다시 |

  보내기 전에 걸러낼 수 있는 것도 먼저 잡습니다 — 두 칸 중 무엇이 비었는지,
  사용자 이름(@orak_food)을 ID 칸에 넣었는지, 토큰이 잘려 붙여넣어졌는지.

- **저장한 토큰인데 칸이 비어 보이던 것** — 토큰은 보안상 화면에 다시 채우지
  않습니다. 그래서 "칸은 비었는데 400 이 난다"로 보였습니다. 다른 API 키 칸과
  똑같이 `✅ 저장됨 (설정) · EAAG00…0000 (104자)` 표시와 [지우기]를 붙였습니다.

- **설정 화면에서 줄이 어긋나 글자가 밀려 보이던 것** — 슬라이더 20px,
  선택 상자 48px, 글상자 52px 이라 두 칸짜리 표에서 같은 줄인데 높이가
  제각각이었습니다. 셋 다 52px 로 맞추고, 이름표가 두 줄로 접혀도 아래 조작기는
  바닥에 맞춰 정렬합니다.

- **[저장] 과 [연결 테스트] 가 세로로 쌓이던 것** — 결과 문구가 길면 버튼 묶음이
  통째로 다음 줄로 밀렸습니다. 단추는 한 줄에, 긴 결과 문구만 아랫줄로 갑니다.

- **발행 실패 문구** 도 같은 방식으로 바꾸고, DB·화면에 남기기 전에 비밀값을
  지웁니다. 재시도 판단은 원문으로 하므로 동작은 그대로입니다.

### 확인한 방법

- 연결 테스트 4가지 경우를 **실제로 서버를 띄워** 눌러 봤습니다
  (빈 칸 / @아이디 / 잘린 토큰 / 실제 호출).
- 자간을 8% 넓혀 **윈도우 맑은 고딕처럼 글자가 넓은 환경을 흉내내고**
  1280·1100·1024px 에서 확인 — 단추 글자 쪼개짐 0, 칸 넘침 0.
- 12개 화면 가로 넘침 0, 자바스크립트 오류 0. 테스트 166개 통과, 빌드 성공.

### 못 한 것 (정직하게)

- **실제 Meta 서버 응답은 못 봤습니다.** 이 작업 환경에서 `graph.facebook.com`
  접속이 막혀 있습니다. 위 표의 문구는 Meta 가 실제로 쓰는 응답 모양으로
  시험을 짜서 확인했고, 화면까지 이어지는 경로도 확인했지만,
  **진짜 토큰으로 눌러 본 것은 아닙니다.**

### 다음에 할 일

1. **회사 PC에서 `업데이트.bat` → `설치파일만들기.bat` → 다시 설치**
   (지금 설치된 것은 8월 23일 판이라 위 수정이 들어 있지 않습니다)
2. ElevenLabs — 목소리 고르기에서 **`기본` 표시가 있는 것**으로 바꾸기
   (무료 요금제는 Voice Library 목소리를 API 로 못 씁니다)
3. OpenAI 크레딧 충전 (지금은 이미지가 자리표시자로 대체됩니다)
4. 릴스 1편 끝까지 만들어 보기
5. 그 다음에 Instagram 연결

---

## 2026-08-23 (일) — 집 · 6차 (관리 기능 · UI 통일 · 실행 환경)

### 추가한 것

- **실패한 작업 정리** — 제작중 화면에서 개별/여러 개/전체 삭제. 확인 창을 거치고,
  지우는 것은 작업 기록뿐(릴스·영상 파일은 남는다). API: `/api/jobs` GET·DELETE
- **Master Reference 보관함** — `assets/character/` 를 폴더로 정리.
  폴더 만들기·이름 바꾸기·삭제, 이미지 개별/여러 개 삭제, 폴더 간 이동, 기준 이미지 선택.
  폴더 삭제는 "폴더만" 과 "이미지까지" 를 나눠 각각 무슨 일이 벌어지는지 알린다.
  설정의 참조 목록이 이동·이름변경·삭제를 따라간다. API: `/api/character/library`
- **정직한 진행률** — 실제 진행률을 셀 수 없는 단계는 숫자를 지어내지 않고 "처리 중"으로,
  100%는 정말 끝났을 때만. 작업별·전체 진행률 분리
- **변경 완료 알림(Toast)** — 무엇이 바뀌었는지 문장으로. 성공은 자동으로 닫히고 오류는 남는다
- **FFmpeg 자동 복구** — `npm run ffmpeg`. doctor 가 실행 파일이 없으면 그 자리에서 받아 고친다
- **업데이트.bat** — 더블클릭 하나로 받아오기→빌드→실행. 경로를 칠 일이 없다

### 고친 것 (모두 내 결함)

- 오류 문구에 API 키가 노출됨 → `redact` 로 화면·로그·DB 를 전부 거른다
- 목소리 ID 칸에 API 키를 붙여넣는 실수 → 저장 단계에서 막고, 두 칸이 뒤바뀐 경우도 알아낸다
- `.env` 한 줄을 통째로 붙여넣어도 동작하게 (`이름=`, 따옴표, 줄바꿈 제거)
- 공급자를 바꿔도 저장되지 않아 엉뚱한 서비스로 연결 테스트가 나가던 문제
- `응답 401` 만 보여 주던 것 → 권한 부족·폐기·한도 초과를 구분해 안내
- 402(요금제 제한)를 키 문제로 오해하지 않게
- sqlite 어댑터가 삭제 건수를 늘 0 으로 돌려주던 문제
- 메뉴 아래쪽(설정)이 잘리던 문제 → 화면 고정 + 메뉴만 스크롤
- **UI 전체 통일** — 글자 크기표를 올려(본문 16px, 보조 15px) 전 화면 적용,
  버튼·입력창 높이 44px 이상, 박스·화면 너비 통일, WCAG AA 대비 확보,
  휴대전화에서 메뉴가 위쪽 가로줄로. 브라우저로 12화면 × 4크기 측정해 확인
- 공통 클래스가 화면별 폭 지정을 눌러 칸이 한 글자씩 뭉개지던 문제 (`@layer components`)

### 지금 상태

테스트 153개 통과, 빌드 통과. 두 브랜치 동일:
`claude/orak-food-reel-studio-2kux9t`, `claude/work-environment-setup-qkurkt`

| 항목 | 상태 |
|---|---|
| Claude (대본) | ✅ 연결됨 |
| ElevenLabs (음성) | ⚠ 키·권한 정상, **무료 요금제라 라이브러리 목소리 사용 불가(402)** |
| 이미지 (OpenAI) | ❌ **크레딧 부족(429)** — 이미지 9장 전부 임시 배경으로 대체됨 |
| FFmpeg | ✅ 자동 복구 넣음 |
| Instagram | ⬜ 아직 연결 안 함 (영상 제작 안정화 후) |

### 다음에 할 일

1. **ElevenLabs** — 목소리 고르기에서 `기본 · 무료 요금제 가능` 표시된 목소리를 고르거나,
   elevenlabs.io 에서 Starter 이상으로 올린다
2. **OpenAI** — platform.openai.com → Billing 에서 크레딧 확인·충전
3. 위 둘이 되면 **진짜 릴스 한 편**을 끝까지 뽑아 본다
4. 그다음 Instagram 연결 (Professional 계정 + 토큰 + `PUBLIC_MEDIA_BASE_URL`)

### 메모

- 회사 PC/집 PC 어디서든 **`업데이트.bat` 더블클릭**이면 최신으로 맞춰진다
- 오늘의 릴스 화면에 메뉴·가격 직접 입력 칸은 아직 없다 (팩트체크가 그만큼 약해짐)

---

## 2026-08-23 (일) — 집 · 5차 (목소리 선택기)

### 추가한 것

- **설정 → ElevenLabs에 목소리 선택기 추가.** Voice ID를 복사해 붙여넣는 대신
  계정에 등록된 목소리 목록을 불러와 카드에서 클릭해 고른다.
  - ▶ 샘플: ElevenLabs 기본 미리듣기(주로 영어) 재생
  - 🇰🇷 한국어: 고른 목소리로 실제 한 문장을 생성해 재생 (크레딧 소모 표시)
  - 라벨(성별/연령/톤)을 한국어로 변환해 표시, 목록이 없으면 Voice Library 안내
  - 키가 없거나 오류면 기존 방식(ID 직접 입력)으로 자동 대체
- API: GET /api/voices (목록), POST /api/voices/preview (한국어 미리듣기 mp3)
- 테스트 3개 추가 (총 45개 통과), 빌드 통과

---

## 2026-08-23 (일) — 집 · 5차 (실제 API 첫 제작 실패 수정)

### 문제

실제 키를 넣고 첫 제작을 돌리자 대본 생성 20%에서 멈춤. API 문제가 아니라 내 설계 결함.

1. **프롬프트에 허용 값 목록이 없었음** — camera_motion / character_action /
   character_expression / character_presence 는 정해진 값만 받는데,
   프롬프트에는 JSON 예시에 값 하나만 들어 있고 목록이 없었다.
   AI가 `zoom_in`, `close-up` 같은 값을 지어냈고 → Zod 검증에서 대본 전체가 폐기 → 3회 재시도 모두 실패.
2. **값 하나 틀리면 전부 버림** — 회복 수단이 없었다.
3. **FOREIGN KEY constraint failed** — 대본 실패 시 `reels` 행이 아직 없는데
   `production_jobs.reel_id`(외래키)를 채우려 해서 실패 기록조차 남지 않았다.

### 고친 것

- `content/prompts.ts` — `ENUM_RULES()` 추가. 허용 값 전체 목록을 프롬프트에 넣고
  "번역·변형·새 값 생성 금지" 명시. 재시도 시 규칙을 한 번 더 못박음
- `content/normalize.ts` **(신규)** — 검증 전에 고칠 수 있는 값은 내부 값으로 맞춤
  - `zoom_in→slow_zoom_in`, `close-up→slow_zoom_in`, `tilt up→push_up` 등
  - `음식 가리키기→손가락으로 음식 가리키기` (2-gram 유사도)
  - 못 맞추면 안전한 기본값: camera_motion→static, action→null, presence→none
  - 장면 시간을 처음부터 다시 이어붙여 "시간 불연속" 오류 원천 차단
  - 해시태그 개수·탐정 판정 필수값도 보정
- `pipeline/run.ts` — `saveJob()`이 reels 행 존재를 확인한 뒤에만 reel_id 연결

### 확인한 것

- 사용자 실패 로그를 그대로 재현한 회귀 테스트 추가 (보정 전 거부 → 보정 후 통과)
- 테스트 54개 통과, 빌드 통과
- `신림동 오첨지 / 신림 / 자동 추천 / 만두탐정 오락이` 로 9단계 전부 완주 (27.3초, 84점)

---

## 2026-08-23 (일) — 집 · 4차 (Windows 실행 오류 수정)

### 문제

설치는 통과했지만 서버가 뜨지 않고 브라우저에 `ERR_CONNECTION_REFUSED`.
원인 두 가지가 겹쳐 있었습니다.

1. `package.json` 의 실행 명령에 `next start -p ${APP_PORT:-3000}` 이라고 써 두었는데
   이건 리눅스/맥 셸 문법이라 Windows(cmd)에서는 그대로 문자로 넘어가
   `'${APP_PORT:-3000}' is not a non-negative number` 오류가 났습니다.
2. `start.bat` 이 서버를 켜기 **전에** 브라우저를 열어, 아직 준비되지 않은 주소에
   접속해 연결 거부가 떴습니다.

### 고친 것

- 실행 명령을 `next dev` / `next start` 로 단순화하고 포트는 **PORT 환경변수**로 전달
  (Next.js 기본 지원 · 세 운영체제 모두 동일하게 동작)
- `scripts/port.mjs` 추가 — `.env` 의 APP_PORT 를 읽어 반환.
  배치 파일 안에 정규식을 직접 쓰면 `^` 가 깨지므로 별도 파일로 분리
- `start.bat` — 서버가 실제로 응답할 때까지 기다렸다가 브라우저를 엶.
  주소·종료 방법을 화면에 안내

### 확인한 것

- PORT=3007 로 기동 → 3007 응답 확인 / 기본 3000 도 정상
- 테스트 42개 통과, 빌드 통과

---

## 2026-08-23 (일) — 집 · 3차 (설치 오류 수정)

### 문제

회사 PC(Node 24)에서 `npm install` 실패.
`better-sqlite3`가 Node 24용 미리 빌드 파일을 제공하지 않아 C++ 컴파일로 넘어갔고,
Visual Studio가 없어서 멈췄습니다. 일반 사용자에게 Visual Studio 설치를 요구하는 건
설계 잘못이라 판단해 원인을 제거했습니다.

### 고친 것

- **DB를 네이티브 모듈 → WebAssembly로 교체** (`better-sqlite3` → `node-sqlite3-wasm`)
  - 어떤 Node 버전에서도 컴파일 없이 설치됨. 이제 C++ 빌드 도구가 전혀 필요 없음
  - 호출부가 86곳이라 `src/lib/sqlite.ts` 호환 계층을 만들어 기존 코드는 그대로 유지
    (prepare/run/get/all, exec, pragma, transaction 동일하게 동작)
  - WAL 모드는 WASM에서 이점이 없어 제거
- `start.bat`: 설치 실패 시 node_modules를 정리하고 **자동으로 한 번 더 시도**,
  실패 시 원인(방화벽 / 드라이브 동기화 폴더 / 백신) 안내
- `doctor.mjs`: 예전 better-sqlite3 찌꺼기가 남아 있으면 경고
- README 오류 해결표에 node-gyp·EPERM 사례 추가

### 확인한 것

- 테스트 42개 통과, `npm run build` 통과
- 샘플 E2E 재실행 — 28.7초 MP4 정상 생성 (85점)
- 서버 기동 후 DB 읽기/쓰기 API 정상 동작

## 2026-08-23 (일) — 집

### 오늘 한 일

**「오락푸드 AI 릴스 자동제작 스튜디오」를 새로 만들었습니다.** (`orak-food-reel-studio/`)

- Next.js 16 + SQLite + FFmpeg 기반, Windows에서 start.bat 더블클릭으로 실행
- 파이프라인 전체 구현: 맛집 조사 → 대본(JSON+Zod 검증) → 팩트체크 → 장면별 이미지 →
  ElevenLabs 음성(장면 시간 자동 보정) → SRT/ASS 자막 → FFmpeg 렌더(1080×1920 H.264/AAC)
  → 썸네일 → 캡션/해시태그 → 품질점수(100점) → 중복검사
- **만두탐정 오락이** 캐릭터 IP 시스템: 사건번호(맛집사건 #NNN), 사건 파일 9장면 구조,
  탐정 판정(별점+한줄판정), 행동 17종·표정 10종 라이브러리, Character Lock(시드+기준이미지),
  음식 60%/캐릭터 40% 화면 규칙, 브랜드 컬러 #E86A3A
- Instagram은 Meta 공식 API만 사용: 컨테이너 생성 → FINISHED 확인 → 발행 → DB 기록,
  재시도/재발행, 토큰 AES-256-GCM 암호화 저장. 팩트체크 실패 콘텐츠는 발행 차단
- 주간 6개 기획(월~토, 일 휴무·설정 가능) → 전체 승인 → 순차 제작, 캘린더 Drag&Drop
- UI 14화면(한국어, 카드형): 홈/오늘의릴스/주간/캘린더/제작중/완성/예약발행/성과/맛집DB/
  벤치마킹/캐릭터/설정/마법사(8단계)/릴스상세(장면 편집+SCENE 단위 재생성)
- Sample Mode: API 키 없이 전체 흐름이 실제 동작(그라데이션 이미지+톤 오디오로 MP4 생성)
- 자동 테스트 34개 통과, npm run build 통과, 샘플 E2E(smoke)로 실제 MP4 렌더 확인

### 추가 작업 — 오락이 기준 이미지 제작

- **Master Reference 7종 제작 완료** (front/side/back, 표정 3종, 캐릭터 시트)
  - 외부 이미지 API 없이 SVG로 직접 디자인 → Chromium으로 고해상도 PNG 렌더
  - 머리 윗부분을 실제 찐만두처럼 **주름(스캘럽) 실루엣**으로 설계 →
    작은 화면에서도 "만두"로 인식됨. 탐정 헌팅캡은 비스듬히 얹어 주름이 가려지지 않게 함
  - 브라운 탐정 모자 / 돋보기 / 오렌지(#E86A3A) 가방 / 탐정 수첩 4종 소품 포함
  - 캐릭터 시트에 비율(18cm, 머리:몸 3:2), 음식 60%/오락이 40% 규칙, 고정 요소, 컬러 6종 명시
- **디자인 원본을 코드로 보관** — `scripts/character/oraki-art.mjs` 수정 후
  `npm run character` 실행하면 7종이 같은 규칙으로 재생성 (SVG 원본은 `assets/character/svg/`)
- **기준 이미지를 실제 생성에 연결** (기존에는 받기만 하고 쓰지 않았음)
  - Gemini: 참조가 있으면 이미지 입력 가능 모델(generateContent)로 전환, 없으면 Imagen
  - OpenAI: 참조가 있으면 images/edits 멀티파트로 전송
  - 사용자가 고른 참조가 없으면 기본 Master Reference 3장을 자동 사용, Lock 해제 시 미전송
- 테스트 8개 추가 (총 42개 통과)

### 시험한 것

- 샘플 E2E 2회: 28초대 MP4 + 한글 자막(ASS/libass) + 썸네일(사건 배지) 정상 렌더
- 발행 상태기계: 컨테이너 FINISHED 후에만 publish, 팩트체크 차단 시 발행 실패 확인
- 다음 슬롯 계산: 일요일 OFF 건너뛰고 월 11:30 배정 확인

### 시험하지 못한 것 (API 키/실계정 필요)

- Claude·ElevenLabs·이미지 API 실호출, 실제 Instagram 발행(Professional 계정+공개 URL 필요)
- Windows start.bat (리눅스 환경 — 문법은 기존 스크립트와 동일 계열)

### 다음에 할 일

1. 회사 PC: `받아오기.ps1` → `orak-food-reel-studio/start.bat` 실행 (회사PC에서_이어하기.md 참고)
2. .env에 실제 API 키 입력 → 설정 화면 [연결 테스트] 4종 확인
3. 오락이 캐릭터 기준 이미지(character_sheet.png 등) 확정해서 캐릭터 메뉴에 업로드
4. 실제 맛집 1곳으로 live 모드 제작 → 비공개 계정으로 시험 발행


---

## 2026-08-22 (토) — 집

### 오늘 한 일

**네이버 블로그 주간 콘텐츠 제작 시스템을 만들었습니다.** (`naver-blog-automation/`)

- 네이버 공식 기능 조사 완료
  - 블로그 글쓰기 API는 **2020년 5월 6일 종료** (광고성 대량 발행 차단 목적)
  - 2026년 6월 출시된 NAVER API HUB에도 글쓰기 기능 없음 (검색 API만)
  - 예약 발행은 공식 지원 → 이걸 기본 방식으로 채택
- 파이프라인 구현: 편성 → 원고 생성 → 사실확인 → 검수(15항목) → 승인 → 발행 준비
- 두 채널 설정, 주제 은행(78개), 금지표현 사전(80개 패턴)
- 번호 선택형 메뉴 + PowerShell 실행 스크립트
- 집/회사 동기화 방법 문서화 (Git 권장 / OneDrive 대안)

### 다음에 할 일

1. **블로그 글 양식 분석** ← 가장 먼저 해야 합니다
   - 두 블로그의 대표 게시물 3~5편을 Claude Code에 보여 주기
   - `config/style_coin.md 를 이 샘플로 분석해서 채워줘` 라고 말하기
   - 그전까지는 원고 양식이 실제 블로그와 다를 수 있습니다
2. 회사 PC에서 `setup.ps1` 한 번 실행
3. 첫 주 원고 12편 만들어 보고 검수 결과 확인
4. 1편만 비공개로 시험 발행해서 서식 확인

### 메모

- **GitHub 푸시가 403으로 막혀 있습니다.**
  Claude의 GitHub 앱에 이 저장소 쓰기 권한이 없습니다.
  https://claude.ai/admin-settings/claude-tag 에서 권한을 주거나
  claude.ai 설정 → 커넥터에서 GitHub 재연결이 필요합니다.
  이게 풀려야 집↔회사 동기화가 됩니다.
- 시험해 본 것: 한글·공백 경로 동작, 주제 4주 무중복, 채널 혼동 차단,
  중복 원고 차단, 금지표현 차단, 예시 숫자와 실제 시세 구분
- 시험하지 못한 것: PowerShell 스크립트(리눅스 환경이라 실행 불가),
  브라우저 예약 등록(네이버 접속 차단), 차트 생성(matplotlib 미설치)

---

## 2026-08-22 (토) — 집 · 2차 작업

### 오늘 한 일

**업로드 방식을 전면 수정했습니다.**

- 스마트에디터 ONE 이 HTML 글쓰기를 지원하지 않는 점을 반영
  → `post.html` 중심 설계 폐기, **미리보기 전용**으로 강등
  → 글마다 `publish/` 패키지 생성 (body.txt, blocks.yaml, images/ 등)
- 이미지는 **포토 업로더로 파일 직접 등록** 방식으로 변경
- 상태를 6단계 → **10단계**로 확장
  (편집기입력 / 비공개저장 / 예약요청 / 예약확인 / 발행 / 발행확인 분리)
- 중복 발행 방지 (본문 해시 + 게시물 번호 기록)
- 예약 충돌 검사 (지난 시각, 요일, 같은 시각 중복, 발행 간격)
- 실패 복구 (체크포인트 10단계, 화면 캡처, 이어하기)
- 발행 직전 **블로그 ID 7항목 화면 재검증**

**오락 5070 경제공부소 양식 분석 완료** (실제 글 1편 실측)

- 제목 49자 / 본문 1,383자 / 평균 문장 35자
- 소제목 4개 고정 구조 (쉽게설명 → 활용 → 실수 → N줄정리)
- 괄호 뜻풀이 17회, 이미지 3장, 해시태그 7개
- 이 값으로 채널별 검수 기준을 보정

**검수기 버그 발견·수정** — 실제 발행 글이 검수에서 막혔는데,
원인이 글이 아니라 제 설정이었습니다. (투자 유의 문구를 실제와 다르게 넣어둠)
실제 문구로 교체하니 15개 항목 전부 통과합니다.

### 다음에 할 일

1. **친절한 코인 설명서 샘플** ← 아직 못 받았습니다
   - 대표 게시물 3~5편을 주시면 경제 채널처럼 실측해서 반영합니다
2. 오락 5070 샘플 2~4편 추가 (소제목 4개가 고정 템플릿인지 확정용)
3. 회사 PC에서 `setup.ps1` 실행
4. 1편만 비공개로 시험 발행

※ 발행 시각은 확정됐습니다 — 코인 07:00 / 경제 07:30, 월~토. (2026-08-22)

### 메모

- **GitHub 푸시 403 계속** — 권한 문제가 아직 안 풀렸습니다.
- 시험 완료: 실제 발행 글 15항목 통과, 중복 차단, 예약 충돌 차단,
  단계 건너뛰기 차단, 승인 전 네이버 접근 차단, 체크포인트 이어하기,
  블록 변환(21블록), 이미지 검사, 메뉴 EOF 무한반복 수정
- 여전히 시험 못 한 것: PowerShell(리눅스 환경), 브라우저 실제 조작(네이버 차단)

---

## 2026-08-22 (토) — 집 · 3차 작업

### 오늘 한 일

**GitHub 업로드 성공** — 커밋 8개가 올라갔습니다. 집↔회사 동기화가 작동합니다.
(Claude의 GitHub 앱은 끝내 설치되지 않아, 사용자가 본인 계정으로 직접 push)

**친절한 코인 설명서 양식 분석 완료** (실제 글 3편 + 이미지 캡처 3장)

가장 중요한 발견: **이 채널은 템플릿이 두 개**입니다.
- A. 모닝 시황 (코인시황뉴스) — 제목 27자 고정, 소제목 4개 고정, 유의문구 있음
- B. 매매팁 (코인투자마인드) — 제목 김, 이모지 먼저, 관련글 링크, 유의문구 없었음

실측: 본문 1,310~1,448자 / 평균 문장 **31자** / 90자 초과 **0개** / 해시태그 13개
이미지: AI 인포그래픽 포스터, 한글 텍스트 대량, 본문 비유를 그대로 시각화

**검수기 버그 또 발견·수정** — 실제 발행 글 3편이 전부 막혔습니다.
- `몰빵 금지`, `풀매수하면 고통스럽습니다` → 말리는 문장인데 유도로 오인
- `큰 손실 없이 지켜냈습니다` → 칭찬인데 수익보장으로 오인
- `떡상`, `불장` → 이 블로그의 고유 어휘인데 은어로 차단
- 유의 문구가 실제와 달라 미검출

문맥 인식을 넣어 고쳤습니다. 걸린 표현 뒤에 `금지`·`고통`·`위험` 같은 말이
가까이 있으면 '말리는 문장'으로 보고 통과시킵니다.
양방향 10개 시험 전부 정확, 실제 글 3편 모두 통과 확인.

### 다음에 할 일

1. **발행 시각 확인** — 코인 실제 발행이 09:38~10:09인데 설정은 07:00.
   어느 쪽으로 할지 정해야 합니다. (경제는 07:30 확정)
2. **매매팁 글 유의문구** — 템플릿 B에 유의문구가 없었음. 넣는 것으로 기본 설정해 둠.
3. 금요일(위험관리) 슬롯 샘플 — 아직 못 봄
4. 오락 5070 샘플 2~4편 추가 (소제목 4개 고정인지 확정용)
5. 회사 PC에서 clone + setup.ps1
6. 첫 주 원고 12편 만들어 검수 돌려보기

### 메모

- 코인 표본 3편은 시황 2 + 매매팁 1. 슬롯별로 더 필요합니다.
- 이미지 캡처에서 AI 생성 한글이 겹쳐 깨진 사례를 봤습니다. 만든 뒤 눈으로 확인 필요.

---

## 2026-08-22 (토) — 집 · 4차 작업

### 오늘 한 일

**GitHub 앱 설치 완료** — 이제 Claude가 직접 push 할 수 있습니다.
더 이상 zip으로 주고받지 않아도 됩니다.

**두 가지 확정** (사용자 확인)

| 항목 | 결정 |
|---|---|
| 코인 발행 시각 | **월~토 08:30** (실제 습관 08:30~09:00 중 이른 쪽) |
| 매매팁 글 유의문구 | **넣는다** (템플릿 A와 동일 문구) |

발행 시각 최종:
- 친절한 코인 설명서: 월~토 **08:30**
- 오락 5070 경제공부소: 월~토 **07:30**

두 채널을 1시간 띄웠습니다. 12편 전부 예약 충돌 검사 통과 확인.

### 다음에 할 일

1. **첫 주 원고 12편 만들어 보기** ← 이제 준비가 끝났습니다
   - `python scripts/schedule_week.py` → `python scripts/generate_week.py`
   - 만든 뒤 검수 돌려서 결과 확인
2. 1편만 비공개로 시험 발행해서 서식 확인
3. 코인 금요일(위험관리) 슬롯 샘플 — 아직 못 봄
4. 오락 5070 샘플 2~4편 추가 (소제목 4개 고정인지 확정용)
5. 회사 PC에서 clone + setup.ps1

### 메모

- 발행 시각을 09:00 으로 바꾸시려면 `config/settings.yaml` 의
  `publish_times.coin` 한 줄만 고치면 됩니다.
- 이제 동기화는 `git pull` / `git push` 로 하시면 됩니다.

---

## 2026-08-22 (토) — 집 · 5차 작업

### 오늘 한 일

**미리 쓸 수 있는 원고 6편을 완성했습니다. 6편 전부 검수 통과입니다.**

| 채널 | 날짜 | 제목 | 상태 |
|---|---|---|---|
| 코인 | 08-25 (월) | 백서 읽는 법 | 통과 |
| 코인 | 08-27 (수) | 조급함이 만드는 세 가지 실수 | 통과 |
| 코인 | 08-28 (목) | 거래소가 문을 닫으면 | 통과 |
| 경제 | 08-25 (월) | ROE 3분 만에 이해하기 | 통과 |
| 경제 | 08-27 (수) | 배당 성향으로 회사 성격 보기 | 통과 |
| 경제 | 08-28 (목) | 나이별 위험의 크기 정하는 법 | 통과 |

본문 1,167~1,240자, 평균 문장 20~24자, 90자 넘는 문장 0개.
글마다 `post.md` / `sources.md` / `image_prompts.md` 를 갖췄습니다.

**검수기 오탐 하나 더 고쳤습니다.**

`100원을 벌었다고 해 봅시다` 를 예시로 못 알아보고 출처를 요구했습니다.
표시어가 `라고 해 봅` 이라 `벌었다고 해 봅시다` 는 안 걸렸습니다.
어미를 가리지 않도록 `~고 해 봅` 으로 넓혔습니다.
(`~고 해 볼게`, `~고 치면`, `쳐 볼게`, `셈 치` 도 함께 추가)

양방향 10개로 시험했습니다.
교육용 예시 5개는 통과, 출처 없는 실제 수치 5개는 전부 검출됩니다.

### 다음에 할 일

1. **이미지 18장 만들기** ← 지금 이것만 하면 발행 준비가 끝납니다
   - 각 글 폴더의 `image_prompts.md` 에 프롬프트와 대체 텍스트가 있습니다
   - 만든 뒤 **한글이 깨지지 않았는지 눈으로 확인**하세요
   - 파일은 그 글 폴더의 `images/` 안에 넣습니다
2. `python scripts/approve.py` 로 승인 → `prepare_publish.py` 로 업로드 패키지 생성
3. 1편만 비공개로 시험 발행해서 서식 확인
4. 시황 글 6편(월·수·토)은 **당일 아침에** 작성합니다
   - `python scripts/generate_week.py --same-day`
5. 코인 금요일(위험관리) 슬롯 샘플, 오락 5070 샘플 2~4편
6. 회사 PC에서 clone + setup.ps1

### 메모

- 코인 3편에 `이전 게시물과 문장 유사도` 확인이 뜹니다.
  마무리 인사가 이 채널의 고정 문구라서 그렇습니다. **막힘이 아니라 확인**이며
  그대로 두셔도 됩니다.
- 미리 쓴 6편은 전부 시황이 아닌 개념·마인드 글이라 수치가 0건입니다.
  그래서 출처 확인이 필요 없습니다.

---

## 2026-08-22 (토) — 집 · 6차 작업

### 오늘 한 일

**시세 기준 출처를 트레이딩뷰로 정했습니다.** (사용자 지정)

시장 주가·시장 동향·시세는 https://kr.tradingview.com/ 을 봅니다.
`config/data_sources.yaml` 에 저장했고, 집·회사 양쪽에 동기화됩니다.

**자료 종류에 따라 필요한 출처 수를 나눴습니다.**

| 종류 | 예 | 필요한 출처 |
|---|---|---|
| 화면에서 읽는 값 | 가격, 지수, 환율, 등락률, 도미넌스 | 트레이딩뷰 한 곳 |
| 사건·발표 | ETF 승인, 금리 인하, 규제 발표 | 서로 다른 두 곳 |

전에는 모든 숫자에 출처 2곳을 요구했습니다.
시세는 차트에서 눈으로 읽는 값이라 한 곳이면 충분하고,
사건·발표는 그대로 2곳을 지킵니다.
아침마다 두 곳을 뒤지지 않아도 되게 하려는 것입니다.

**메뉴 9번 '오늘 시세 확인하기' 를 넣었습니다.**

열어 볼 트레이딩뷰 주소와, `sources.md` 에 붙여 넣을 표가 나옵니다.
값은 화면을 보고 직접 채우십니다. 프로그램이 대신 채우지 않습니다.

### 다음에 할 일

1. **트레이딩뷰 주소 8개 확인** ← 5분이면 됩니다
   - `python scripts/quotes.py` 를 돌리면 목록이 나옵니다
   - 앞에 `?` 가 붙은 것은 제가 확인 못 한 주소입니다
   - 열어 보시고 `config/data_sources.yaml` 의 `url` 을 고친 뒤
     `verified: false` 를 `true` 로 바꿔 주세요
2. 이미지 18장 만들기 (각 글 폴더 `image_prompts.md` 참고)
3. `approve.py` → `prepare_publish.py`
4. 1편만 비공개로 시험 발행
5. 회사 PC에서 clone + setup.ps1

### 메모

- **Claude 는 트레이딩뷰에 접속할 수 없습니다.** 작업 환경의 네트워크 정책에
  막혀 있습니다. (네이버·업비트·코인게코·한국은행·KRX 도 같이 막혀 있습니다)
  시세는 사람이 읽어서 적어 주셔야 합니다.
- 검사기는 출처 칸에 적힌 이름을 보고 시세인지 사건인지 나눕니다.
  즉 어디서 봤는지는 **적는 사람을 믿습니다.**
  뉴스에서 본 값을 '트레이딩뷰'라고 적으면 시세로 보고 통과시킵니다.
  편의를 위한 규칙이지 부정을 막는 장치가 아닙니다.
- 시험: 시세/사건 구분 10개, 기존 원고 6편 회귀 — 전부 통과.

---

## 2026-08-22 (토) — 집 · 7차 작업

### 오늘 한 일

**폴더를 두 개로 나누기로 정했습니다.** (사용자 확정)

| 폴더 | 무엇을 | 동기화 |
|---|---|---|
| `G:\내 드라이브\ORAK-작업\` | 문서·보고서 | 구글 드라이브 |
| `내 문서\블로그작업\` | 블로그 프로젝트 | 깃허브만 |

문서는 드라이브에 둬도 괜찮습니다. 프로그램 폴더만 밖에 둡니다.

**실수를 프로그램이 막도록 넣었습니다.**

블로그 폴더가 구글 드라이브·원드라이브·드롭박스·아이클라우드·
네이버 마이박스 안에 있으면 실행할 때 경고하고 멈춥니다.

- `menu.py` — 시작 화면에서 경고
- `받아오기.ps1` · `올리기.ps1` · `setup.ps1` — 경고 후 계속할지 물어봄

경로에 클라우드 폴더 이름이 있는지 봅니다.
`D:\드라이브연습` 처럼 이름만 비슷한 건 통과합니다. (12개 시험 전부 정확)

**회사 PC 설치 안내서를 만들었습니다.** (`회사PC_처음설치.md`)

git 설치 → 실행 정책 → clone → setup.ps1 순서를 한 단계씩 적었고,
clone 명령은 실제로 실행해 동작을 확인했습니다.
회사 보안으로 설치가 막힌 경우의 대안 두 가지도 넣었습니다.

### 다음에 할 일

1. **회사 PC에서 clone 해 보기** ← `회사PC_처음설치.md` 보고 그대로
2. 트레이딩뷰 주소 8개 확인 (`python scripts/quotes.py`)
3. 이미지 18장 만들기
4. `approve.py` → `prepare_publish.py`
5. 1편만 비공개로 시험 발행

### 메모

- 저장소가 **공개(public)** 입니다. 발행 전 원고가 인터넷에 보입니다.
  신경 쓰이시면 Settings → Danger Zone 에서 비공개로 바꿀 수 있습니다.
  바꾸면 회사 PC에서 받을 때 깃허브 로그인을 한 번 물어봅니다.
- PowerShell 은 리눅스 환경이라 실행해 보지 못했습니다.
  괄호·따옴표는 눈으로 확인했고, BOM·CRLF 는 유지했습니다.

---

## 2026-08-26 (수) — 집

### 오늘 한 일

**저장 위치 규칙을 프로그램이 강제하도록 바꿨습니다.** (사용자 지정)

전에는 클라우드 폴더에서 "계속할까요?" 하고 물었습니다.
이제는 **묻지 않고 멈춥니다.**

| 무엇을 | 어디에 | 동기화 |
|---|---|---|
| 문서·보고서 | 구글 드라이브 / 원드라이브 | 클라우드 |
| **블로그 프로그램** | **사용자 PC** (예: `C:\작업\블로그작업`) | **깃허브만** |

막는 곳: 원드라이브 · 구글 드라이브 · 드롭박스 · 아이클라우드 ·
네이버 마이박스 · 네이버 클라우드

**윈도우 '문서 폴더 이전' 도 잡습니다.**
원드라이브가 문서 폴더를 통째로 가져가면 경로에 OneDrive 라는 글자가
없을 수 있습니다. 그래서 환경변수(`OneDrive` 등)로도 확인합니다.

검사하는 곳 4군데 — `menu.py`, `받아오기.ps1`, `올리기.ps1`, `setup.ps1`

빠져나갈 구멍은 `settings.yaml` 의 `storage.allow_cloud_folder` 하나이며
기본값은 꺼짐입니다.

**이미지폴더열기.bat 을 만들었습니다.**

윈도우 파일 대화상자에 짧은 경로를 넣으면 "이 위치를 열 수 없습니다"
오류가 납니다. 번호만 누르면 그 글의 `images` 폴더가 탐색기로 열립니다.

### 다음에 할 일

1. **어제(08-25) 경제 글 이미지 2장 넣기** — `roe-basic-01.png` `roe-basic-02.png`
2. 넣은 뒤 `올리기.ps1` → 검수·발행 패키지 갱신
3. 오늘(08-26 수) 시황 2편은 당일 작성
4. 트레이딩뷰 주소 8개 확인 (`python scripts/quotes.py`)

### 메모

- 사장님 현재 경로는 `C:\Users\USER\Documents\블로그작업\` 로
  **원드라이브 밖입니다.** 지금 그대로 두셔도 됩니다.
- 회사 PC 작업(릴스 스튜디오 등)을 합쳤습니다.
  `image_prompts.md` 충돌 하나는 원격(더 최신) 쪽을 택했습니다.
- 시험: 클라우드 판정 12건, 차단 동작 2건 — 전부 정확.
  PowerShell 은 리눅스 환경이라 실행하지 못했고 구문만 확인했습니다.
