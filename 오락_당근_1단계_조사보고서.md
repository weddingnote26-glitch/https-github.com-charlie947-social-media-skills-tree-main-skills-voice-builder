# 오락_당근_컨텐츠 자동 생성 프로그램 — 1단계 조사 보고서

- **공식 작업명**: `오락_당근_컨텐츠 자동 생성 프로그램`
- **조사 일시**: 2026-08-31
- **조사 범위**: 이 저장소 전체 + 모든 브랜치 + 전체 커밋 기록 + 컨테이너 파일시스템
- **조사 방식**: 읽기 전용. **아무것도 고치거나 지우거나 옮기지 않았습니다.**

---

## 결론 (가장 중요)

> ## ⚠️ 당근 콘텐츠 자동 생성 프로그램은 이 저장소에 없습니다.

이 저장소에 들어 있는 것은 **네이버 블로그 프로그램**과 **릴스(영상) 프로그램** 두 개뿐입니다.
둘 다 당근 프로그램이 아닙니다.

절대 보호 규칙에 따라 **비슷한 폴더를 임의로 고르지 않았고, 아무것도 수정하지 않았습니다.**

> 작업 대상 경로를 찾지 못하면 비슷한 폴더를 임의로 선택하지 말고 보고합니다.

---

## 지금 저장소에 있는 폴더

| 구분 | 경로 | 용도 | 최근 수정일 | 파일 수 | 처리 방안 |
|---|---|---|---|---|---|
| 네이버 블로그 | `naver-blog-automation/` | 네이버 블로그 주간 원고 생성·검수·예약발행 (코인/주식 **2채널**) | 2026-08-29 | 240 | **당근 프로그램 아님 → 변경하지 않음** |
| 릴스·영상 | `orak-food-reel-studio/` | 인스타 릴스 AI 자동제작 (대본→음성→자막→MP4) | 2026-08-29 | 219 | **보호 대상 → 절대 변경 금지** |

### `naver-blog-automation` 이 당근 프로그램이 아닌 근거

| 확인한 것 | 실제 내용 | 당근 프로그램이라면 |
|---|---|---|
| 콘텐츠 분야 | `coin`, `stock` — **2개** | 6개 분야여야 함 |
| 발행 대상 | `blog.naver.com/dylankim26`, `blog.naver.com/playorak` | 당근 동네생활 |
| 발행 코드 | `scripts/browser_publish.py`, `config/naver_selectors.yaml` (네이버 에디터 선택자) | 당근 게시 흐름 |
| 프로그램 이름 | `네이버블로그도우미.spec`, `start-naver-blog-automation.bat` | 당근 관련 이름 |

`config/style_stock.md` 와 `samples/stock/*.md` 에 "당근" 이라는 낱말이 나오지만,
이는 **블로그 글 본문 안의 모객 문단**입니다 — 당근에서 운영하는 오프라인 모임을 블로그 글에서 홍보하는 문장이고,
프로그램이 당근에 글을 올린다는 뜻이 아닙니다.

### 저장소 전체에서 찾은 단 하나의 당근 경로

```
orak-food-reel-studio/src/app/settings/page.tsx : 321
  placeholder="예: C:\Users\USER\Desktop\오락_당근_콘텐츠\character\oraki\v1"
```

- 이것은 **입력칸의 예시 문구(placeholder)** 일 뿐, 실제 폴더가 아닙니다.
- 다만 이 문구는 **사용자 PC 바탕화면에 `오락_당근_콘텐츠` 폴더가 있다**는 것을 알려줍니다.
- 그 폴더는 깃허브에 올라와 있지 않아 이 작업 환경에서는 열어볼 수 없습니다.

### 찾아본 곳 (모두 결과 없음)

- 저장소 전체 파일 이름 · 파일 내용
- 모든 브랜치 (`claude/carrot-content-generator-refactor-k8pg24`, `claude/work-environment-setup-qkurkt` — 두 브랜치 내용 동일)
- 전체 커밋 기록에서 한 번이라도 추가된 적 있는 모든 파일 이름
- 커밋 메시지 전체
- 컨테이너 파일시스템 전체 (`당근`, `carrot`, `karrot`, `daangn`)

---

## 보호 대상 (건드리지 않음)

| 보호 경로 | 내용 | 상태 |
|---|---|---|
| `orak-food-reel-studio/` 전체 | 릴스·영상·Remotion·오락이 캐릭터 | 파일 219개, 수정 시간 그대로 |
| `orak-food-reel-studio/src/app/api/reels/` | 릴스 API | 그대로 |
| `orak-food-reel-studio/src/app/reel/` | 릴스 화면 | 그대로 |
| `orak-food-reel-studio/assets/video/` | 영상 자료 | 그대로 |
| `orak-food-reel-studio/assets/character/` | 오락이 캐릭터 원본 | 그대로 |
| `naver-blog-automation/` 전체 | 네이버 블로그 프로그램 | 파일 240개, 수정 시간 그대로 |

작업 전 기준선(파일 수·크기·수정 시간)을 기록해 두었고, 작업 후 비교해 변동이 없음을 확인했습니다.

---

## 다음 단계 — 당근 폴더를 저장소에 올리기

사용자 PC 바탕화면의 `오락_당근_콘텐츠` 폴더를 저장소에 올려 주시면,
그 **실제 프로그램**을 대상으로 6개 분야 통일 · 이전작업 보관 · 중복방지 작업을 이어서 진행합니다.

### 하시는 방법

1. 이 폴더의 **`당근폴더_가져오기.ps1`** 을 마우스 오른쪽 클릭 → **"PowerShell에서 실행"**
2. 화면에 나오는 안내를 따라가시면 됩니다.
3. 끝나면 같은 폴더의 **`올리기.ps1`** 을 실행해 깃허브에 올려 주세요.
4. Claude 에게 **"당근 폴더 올렸습니다"** 라고 알려 주세요.

### `당근폴더_가져오기.ps1` 이 지키는 것

| 지키는 것 | 어떻게 |
|---|---|
| 원본을 지우지 않음 | 복사만 합니다. 원본은 그대로 둡니다. |
| 덮어쓰지 않음 | 같은 이름이 있으면 건너뛰고 기록에 남깁니다. |
| 조사 먼저 | 무엇이 들어 있는지 먼저 보여주고, 확인을 받은 뒤에만 복사합니다. |
| 민감정보 보호 | `.env`, `*.key`, `*.pem`, 로그인 쿠키, `settings.yaml`, `secrets.yaml` 은 복사하지 않습니다. |
| 숏츠·유튜브 보호 | 숏츠·쇼츠·shorts·유튜브·youtube·릴스·reels·영상·video·remotion·oraki 폴더는 복사하지 않습니다. |
| 대용량 제외 | mp4·mov·mp3·wav·psd·zip 등은 복사하지 않습니다. |
| 기록을 남김 | `오락_당근_폴더조사결과.txt`, `오락_당근_가져온파일목록.csv` 에 무엇을 어떻게 했는지 적습니다. |
| 임의 선택 안 함 | 당근 폴더를 못 찾으면 비슷한 폴더를 고르지 않고 멈춘 뒤 알려 드립니다. |

### 폴더가 다른 곳에 있다면

스크립트가 바탕화면·문서·사용자 폴더·`C:\작업`·`D:\작업` 을 찾아봅니다.
이 중에 없으면 멈추고 알려 드립니다. 그때는 그 폴더를 파일 탐색기에서 열고
**주소창의 경로를 복사해서** 알려 주시면 됩니다.

---

## 아직 하지 않은 것 (확인 필요)

당근 프로그램 실물이 없어 아래는 **모두 진행하지 못했습니다.**

| 항목 | 상태 | 사유 |
|---|---|---|
| 6개 콘텐츠 분야 통일 | 확인 필요 | 대상 프로그램 없음 |
| 이전 작업물 `archive_hidden` 보관 | 확인 필요 | 대상 자료 없음 |
| 보관 폴더 노출 차단 | 확인 필요 | 대상 프로그램 없음 |
| 중복·덮어쓰기 방지 강화 | 확인 필요 | 대상 프로그램 없음 |
| 버전 최신화 · `CHANGELOG.md` | 확인 필요 | 기존 버전을 확인할 수 없음 |
| 실행 테스트 | 확인 필요 | 실행할 프로그램 없음 |

버전은 실물을 확인한 뒤 정합니다. 기존 버전을 찾지 못하면 지시대로 `1.0.0` 으로 시작합니다.
**임의로 높은 버전을 붙이지 않습니다.**
