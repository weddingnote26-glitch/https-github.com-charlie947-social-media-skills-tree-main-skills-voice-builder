# 회사 PC에서 오락푸드 릴스 스튜디오 시작하기

집에서 한 작업을 회사 PC로 가져오는 방법입니다.
**처음 한 번만 15분**, 그다음부터는 더블클릭 두 번입니다.

> 이 문서는 **오락푸드 AI 릴스 스튜디오** 용입니다.
> 블로그 프로젝트는 `회사PC_처음설치.md` 를 보세요.

---

## 먼저 알아두실 것 — 넘어가는 것과 안 넘어가는 것

깃허브에는 **프로그램**만 올라갑니다. **비밀값과 만든 결과물은 올라가지 않습니다.**
일부러 그렇게 해 둔 것입니다 — API 키가 인터넷에 노출되면 안 되니까요.

| | 회사 PC로 넘어가나요 |
|---|---|
| 프로그램 전체 (화면·기능·설정 구조) | ✅ |
| 만두탐정 오락이 기준 이미지 7종 | ✅ |
| 대본 틀·해시태그·품질 기준 | ✅ |
| **API 키 3개** (Claude · ElevenLabs · 이미지) | ❌ **다시 넣어야 합니다** |
| 맛집 DB·릴스 기록 | ❌ 그 PC에만 남습니다 |
| 완성된 영상 파일 | ❌ 그 PC에만 남습니다 |

**API 키는 회사 PC에서 한 번 더 넣어 주셔야 합니다.** 5분이면 됩니다.
키를 넣는 곳은 아래 4단계에 있습니다.

---

## 1단계 — Node.js 설치 (5분, 한 번만)

이 프로그램은 Node.js 로 돌아갑니다. (블로그 프로젝트의 파이썬과는 별개입니다)

**시작 → `powershell` → PowerShell 실행** 후:

```powershell
node --version
```

| 나온 결과 | 할 일 |
|---|---|
| `v20.x` 이상 | 2단계로 |
| 아무것도 없거나 `v18` 이하 | 아래에서 설치 |

### 설치하기

https://nodejs.org → **LTS** 단추를 눌러 내려받아 실행합니다.
선택지는 **전부 Next** 로 두시면 됩니다.

설치가 끝나면 **PowerShell 창을 닫고 새로 여세요.** (안 그러면 계속 못 찾습니다)

---

## 2단계 — git 설치와 실행 허용

`회사PC_처음설치.md` 의 1~2단계와 같습니다. 이미 하셨으면 건너뛰세요.

```powershell
git --version
```

없으면 https://git-scm.com/download/win 에서 설치.
그리고 한 번만:

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

---

## 3단계 — 작업물 받아오기 (3분)

### 이 PC에서 처음이라면

```powershell
cd ~\Documents
git clone https://github.com/weddingnote26-glitch/https-github.com-charlie947-social-media-skills-tree-main-skills-voice-builder.git 블로그작업
```

`내 문서\블로그작업` 폴더가 생깁니다.

> ⚠ 구글 드라이브·원드라이브 **안에 두지 마세요.** 작업 기록(.git)이 깨집니다.
> `내 문서` 아래면 됩니다.

### 이미 받아 둔 적이 있다면

폴더 안의 **`업데이트.bat` 더블클릭**이 전부입니다.
(`내 문서\블로그작업\orak-food-reel-studio\` 안에 있습니다)

---

## 4단계 — 실행하고 API 키 넣기 (7분)

### ① 프로그램 켜기

`내 문서\블로그작업\orak-food-reel-studio` 폴더를 열고
**`start.bat` 더블클릭.**

처음에는 5~10분 걸립니다. 알아서 다 합니다:

```
[1/4] 필요한 프로그램 설치
[2/4] 환경 점검 — .env 만들기 · 한글 폰트 받기 · FFmpeg 받기
[3/4] 빌드
[4/4] 서버 시작 → 브라우저 자동 열림
```

> 검은 창은 **닫지 마세요.** 닫으면 프로그램이 꺼집니다.
> "Windows의 PC 보호" 경고가 뜨면 **추가 정보 → 실행**.

### ② API 키 3개 넣기

브라우저에서 `설정` 메뉴로 갑니다. 세 곳에 키를 넣고 각각 **저장**:

| 칸 | 키 받는 곳 | 형식 |
|---|---|---|
| **Claude API 키** | console.anthropic.com → API Keys | `sk-ant-…` |
| **ElevenLabs API 키** | elevenlabs.io → 프로필 → API Keys | `sk_…` |
| **이미지 API 키** | platform.openai.com → API keys | `sk-…` |

> `.env` 한 줄을 통째로 붙여넣어도 됩니다 — 이름과 따옴표는 알아서 걸러냅니다.
> 키는 **암호화되어 이 PC에만** 저장됩니다. 깃허브로 올라가지 않습니다.

각 칸 아래 **[연결 테스트]** 를 눌러 `✅ 연결 성공` 을 확인하세요.

### ③ 목소리 고르기

설정 → **목소리 고르기** 에서 원하는 목소리를 클릭 → 저장.

> ⚠ ElevenLabs **무료 요금제**면 `기본 · 무료 요금제 가능` 표시가 붙은 것만 고를 수 있습니다.
> 라이브러리 목소리를 쓰려면 Starter 이상(월 $5)이 필요합니다.

### ④ 실행 모드

설정 맨 위 **⚡ 실행 모드**:

- **연습 모드** — API 키 없이 전체 흐름만 확인 (무료)
- **실제 모드** — 진짜 AI로 만듭니다 (요금 발생)

키를 다 넣으셨으면 **실제 모드**로 바꾸세요.

---

## 5단계 — 끝. 그다음부터는

| 언제 | 무엇을 |
|---|---|
| **작업 시작 전** | `업데이트.bat` 더블클릭 |
| **매일 쓸 때** | `start.bat` 더블클릭 |

`업데이트.bat` 이 받아오기 → 빌드 → 실행까지 한 번에 합니다.
명령창에 경로를 칠 일이 없습니다.

### 꼭 지킬 것

1. **한 번에 한 PC에서만** 작업합니다.
2. 자리를 뜨기 전에 **집/회사 어느 쪽이든 올려두세요.**
   (코드를 고친 경우에만 해당합니다. 릴스를 만든 것은 올라가지 않습니다)
3. 시작하기 전에 **`업데이트.bat`** 을 먼저 돌리세요.

---

## 자주 겪는 문제

| 증상 | 해결 |
|---|---|
| `node` 를 인식할 수 없습니다 | 설치 후 PowerShell 창을 새로 여세요 |
| `FFmpeg를 찾을 수 없습니다` | `start.bat` 을 한 번 더 실행하면 자동으로 받아옵니다. 계속 안 되면 검은 창에서 `npm run ffmpeg` |
| `EADDRINUSE :::3000` | 이미 켜져 있습니다. 검은 창을 닫고 다시 실행하세요 |
| 브라우저가 안 열림 | 주소창에 `localhost:3000` 직접 입력 |
| `설치에 실패` | 회사 방화벽일 수 있습니다. 아래 "회사 보안" 을 보세요 |
| 화면이 예전 그대로 | `업데이트.bat` 을 쓰지 않고 `start.bat` 만 눌렀을 때 생깁니다 |
| `fatal: not a git repository` | 폴더 밖에서 명령을 친 것입니다. 명령창 대신 `업데이트.bat` 을 쓰세요 |

---

## 회사 보안 때문에 막힌다면

**설치가 아예 안 되는 경우** — https://claude.ai/code 에서 이 저장소를 열면
회사 PC에 아무것도 설치하지 않고 브라우저 안에서 작업할 수 있습니다.
다만 **영상 만들기는 그 PC의 프로그램에서만** 됩니다.

**깃허브만 막힌 경우** — 저장소 페이지에서 `Code → Download ZIP` 으로 받을 수 있습니다.
받기만 되고 올리기는 안 됩니다.

**FFmpeg 다운로드만 막힌 경우** —
https://www.gyan.dev/ffmpeg/builds/ 에서 `ffmpeg-release-essentials.zip` 을 받아
압축을 풀고 `bin` 폴더를 시스템 PATH 에 추가한 뒤 프로그램을 다시 켜세요.

---

## 요약 (이것만 기억하세요)

```
처음 한 번   Node.js 설치 → git clone → start.bat → 설정에서 API 키 3개
그다음부터   업데이트.bat 더블클릭
```
